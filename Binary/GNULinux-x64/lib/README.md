This would install the LLVMgold.so which is a faster linker than ld, see The LLVM gold plugin, and we know it resides in /usr/lib/llvm-16/lib. We will later copy LLVMgold.so to our bundled directory so that we can link with CMake LTO (https://stackoverflow.com/a/77396272/6658943) where gold linker is required.