# Pipeline Health — 2026-07-30 scheduled tick 35

- Terminal probe in this scheduled runspace continues to be rejected before launch: `status=pending_approval`, `exit_code=-1`, `pattern_key=tirith:unknown`; no executable command ran, so no fresh scan/build/run/log/dump/validator/structural/visual evidence was produced.
- `docs/PENDING_PICK.md` is still authoritative: v126 is **PARENT-EVIDENCE-GATED** and v127 is **CURRENT TICK BLOCKED**; the six-role dispatcher correctly did not start another file-only cycle and this outer watchdog did not nudge it (the inner pipeline is already terminal-exhausted per `docs/PIPELINE_OUTER_ESCALATION_2026-07-29.md`).
- File-only inspection reconfirms: dumps directory newest stamp remains `20260727_000706-08` (~40+ hours stale), `Engine/Source/Runtime/Binary/Debug/TestReSTIR_GI_Temporal.log` first line is `[2026-07-27 00:07:01.005]` (same staleness), no `PIPELINE_GOAL_DONE_*.md`, no `PIPELINE_NUDGE_*.md`; the v124 marker chain's runtime checks remain unverified.
- Final-goal acceptance is therefore 0/7 verified: clean Debug build, fresh `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM>=8` run, fresh-log exclusion of `Cannot open a command list that is already open`, fresh-log exclusion of Vulkan `ERROR`/`VUID-VkDescriptorImageInfo-imageLayout-00344`, newest-group validator PASS, structural image statistics, and direct visual Sponza inspection at sane exposure.
- No source/test edit, role marker cycle, completion marker, Kanban action, commit, push, history rewrite, or fabricated PASS was produced. Writable surface used: this `docs/PIPELINE_HEALTH_2026-07-30_tick35.md` (append-only audit per Hard rule #7 — never silently exit).
- Resume requires parent terminal evidence (Option B recipe from `docs/PIPELINE_OUTER_ESCALATION_2026-07-29.md`) or reconfiguration of the inner cron job's `enabled_toolsets` to include `"terminal"` (Option A). Stale artifacts and scalar validator results on the existing dump group cannot satisfy the goal per gpu-rendering-bisect-debug § "Don't accept 'PASS' when the symptom is...".
