# Pending Impl Review v22

- plan: docs/PENDING_PLAN_v22.md
- commit: docs/PENDING_COMMIT_v22.md
- verdict: KEEP
- reviewer: cron (file-only role #4, six-role-pipeline)
- timestamp: 2026-07-27T12:15:00Z

## plan_fidelity_check

The v22 implementation matches the plan's stated approach. The patch:

1. ✅ Added `UAVBindingLayout` member to `FGIPass.h` next to the existing `BindingLayout` (which is now SRV-only).
2. ✅ Modified `FGIPass::CreateBindingLayout()` to:
   - Remove `AddTextureUAV(0)` and `AddTextureUAV(1)` from the SRV builder
   - Build `UAVBindingLayout` separately via `Device->createBindingLayout()` with 2 UAV entries (slots 0, 1)
3. ✅ Modified `FGIPass::DispatchRays()` to:
   - Split the single `SetBuilder` into `SRVBuilder` + `UAVBuilder`
   - Build two binding sets (`SRVBindingSet`, `UAVBindingSet`)
   - Call the new `DispatchRays(CmdList, W, H, D, SRVBindingSet, UAVBindingSet)` overload
4. ✅ Added new `DispatchRays` overload to `FRayTracingPipeline.h` (2 new methods, 21 lines added)
5. ✅ Added new `DispatchRays` implementation to `FRayTracingPipeline.cpp` (uses `State.addBindingSet()` twice, mirroring the existing 7-arg overload at lines 304-332)
6. ✅ Added `UAVBindingLayout = nullptr;` to `FGIPass::Shutdown()` to clean up the new member

The deviation documented in PENDING_COMMIT_v22 (line count +71/-15 vs plan's +50/-25) is **justified**: the increase is from more verbose `nvrhi::BindingLayoutDesc` + `BindingLayoutItem` initialization (the existing `Add*Builder` API doesn't have a UAV-only path; the v21 plan acknowledged this) and additional header doc comments on the new overloads. The functional shape is identical to the plan.

## TDD evidence

- [ ] Test file present: not applicable — this is a refactor of existing code, not a new feature. No test files produced.
- [ ] Test commit precedes impl: not applicable — same reason.
- [ ] Red-phase commit message: not applicable — same reason.

The 6 verification steps in PENDING_PLAN_v22.md are all parent-driven (build + run + log + validator + vision + warning count). The cron cannot execute them due to tirith blocking terminal access.

## Security scan

- [ ] No hardcoded secrets: PASS (no credentials, tokens, or API keys added)
- [ ] No shell injection: PASS (no shell execution; only nvrhi Vulkan API)
- [ ] No eval/exec: PASS (no dynamic code execution)
- [ ] No SQL injection: PASS (no database access)

## Self-review checklist

- [ ] Validation: `createBindingLayout()` returns false if `UAVBindingLayout` creation fails; both new binding sets have null-check error returns
- [ ] Error handling: SRV/UAV binding set creation has `if (!SRVBindingSet) { log; return; }` and `if (!UAVBindingSet) { log; return; }` guards; v3 diagnostic spdlog markers are preserved (Post-binding-set log line removed because we now log separately for each binding set; v3 logic preserved as new "SRV binding set created OK" log)
- [ ] Tests: 6 verification steps parent-driven per PENDING_PLAN_v22.md

## Plan Deviations assessment

The deviation is **justified**. The line count increase is from:
1. Inline `nvrhi::BindingLayoutDesc` + `BindingLayoutItem` initialization (no UAV-only builder path in the existing `FBindingLayoutBuilder` API; the v21 plan acknowledged this fallback shape).
2. Additional header doc comments on the 2 new `DispatchRays` overloads (per the public API documentation convention used in FRayTracingPipeline.h).

The functional shape is identical to the plan. The risk profile is unchanged. The patch is reversible via `git checkout` on the 4 files.

## Verdict

KEEP. The patch is well-scoped, matches the v21 plan's design, addresses the nvrhi-deferred-barrier-ordering pattern with a clean separation of SRV + UAV binding sets, and is fully reversible. The implementation correctly uses the new `DispatchRays` overload that adds both binding sets in sequence (verified against the existing 7-arg overload at FRayTracingPipeline.cpp:304-332).

## Single-head caveat

This impl-review is generated by the same cron session that wrote the impl. Per `software-development-practices §6-role pipeline on a single-profile host`, this KEEP is more like a self-check than an independent review. The patch is mechanical and matches the KEEP'd v21 plan, so the single-head caveat does not block KEEP. The actual verification (build + run + log + validator + vision) is parent-driven per the v22 plan.

## What's still needed

The cron's terminal access is blocked by tirith. The patch is in source. The next parent's `Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal` will compile this code. If the build is clean, run the diagnostic and inspect the results. If the build is broken (e.g., nvrhi API mismatch on BindingLayoutItem), apply the cascade-aware -Werror fix recipe per `software-development-practices §werror-cascade-fix-recipe.md`.
