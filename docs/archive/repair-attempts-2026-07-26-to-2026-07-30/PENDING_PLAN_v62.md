# Pending Plan v62
- task: v62 — update TestReSTIR_GI_Temporal_Data/README.md to document the post-six-role-pipeline diagnostic surface (modes 6/7/8/9/10/11/12/15 + default gray + alpha sentinel v28 + encoder fix v41 + helper scripts decode_v38_evidence.py + dump_pixelstats.py + fresh-evidence-scan.sh + run_rgi_diagnostic.sh). This is genuinely useful (the README currently says "modes 0..5, 13, 14" which is stale since v13-v19 added 6/7/8/9/10/11/12/15 + default gray, since v28 added the alpha-channel sentinel, since v41 fixed the encoder to preserve alpha, and since v43 expanded the fresh-evidence helper). It is a doc-drift fix identical in shape to v7/v8/v14/v54 (pure text replacement, zero behavior change, zero risk of regression). It directly serves the parent's recovery recipe by making the diagnostic surface discoverable from README.md without grepping 30+ vN entries in PENDING_PICK.md.
- source: no bundle — direct edit in working tree
- approach: 1 README file modified, +60 / -10 net lines (8-line section per diagnostic surface: mode table, alpha sentinel, encoder fix, helper scripts). All edits are textual insertions/replacements of stale passages; no source-code touched.
  1. **Fix stale "modes 0..5, 13, 14" claim** by extending the "Debug visualization" section to enumerate modes 6 (per-pixel gradient sentinel) / 7 (TraceRay-bypass primary) / 8 (TraceRay-only) / 9 (diffuse * 1.5) / 10 (debugMode cbuffer reach) / 11 (View cbuffer reach) / 12 (AmbientColor only) / 15 (debugMode raw) + default gray + unconditional alpha sentinel (v28). Add a 1-line attribution per mode so future readers can trace each sentinel to its specific commit comment in source.
  2. **Add "Helper scripts" section** mentioning `validate_restir_gi.py` (4 checks + check_alpha_sentinel), `dump_pixelstats.py` (fast first-look on stale/fresh dumps, alpha-aware since v40), `decode_v38_evidence.py` (v38 cerr-line evidence auto-classification), `fresh-evidence-scan.sh` (read-only patch-presence inventory, expanded to 27 entries in v43), `run_rgi_diagnostic.sh` (one-shot 10-mode evidence-capture runner, dump-rotation fixed in v23). Each script gets 1-line description + canonical use path so parent doesn't have to grep the working tree.
  3. **Fix stale "HLVM_PT_DEBUG_MODE=N from the GIPathTracing.hlsl debug code (modes 0..5, 13, 14) is honored — see the path-trace debug session for the meaning of each mode" claim** by adding a forward reference: "Cumulative diagnostic surface from the six-role-pipeline cycles 2026-07-27 is documented in docs/PIPELINE_HEALTH_2026-07-28.md. The decisions in v32/v33/v42/v13a decision matrices (see docs/PENDING_PLAN_*.md) tell parent what to do with mode-6/7/8/9/10/11/12/15 evidence."
- diff_estimate: 1 file modified, +50 / -8 lines net (60-line new content - 8-line replaced)
  - README.md: +50 / -8 (extend Debug visualization section, add Helper scripts section, add forward-reference paragraph)
- skip_plan_review: no — README updates are user-visible and frequently overwritten in commits; deserves a quick critical pass for tone/grammar and consistency with existing sections.
- test_strategy: role #5 (tester) verifies the README renders sensibly in markdown; role #6 (testing-verifier) confirms no source-code change (the test surface must remain unchanged). Both checks pass via static inspection (no rebuild needed).
- risks:
  - *README modifications are content-discoverable*. Anyone using the test data dir will read this file. Doc-drift honesty is more important than brevity here; the current stale "modes 0..5, 13, 14" line actively misleads anyone diagnosing the renderer.
  - *Mode numbers are stable.* All sentinels in v13/v17/v18/v19 are in the SAME positions in BOTH HLSL copies today (verified re-inspection at lines 583-694). The README enumeration matches source.
  - *Helper scripts are stable.* All 5 helper scripts present and intact (verified via `search_files` `pattern="*.py"` and `pattern="*.sh"` both show them). Their canonical paths in the README match the on-disk paths exactly.
  - *No new behavior.* README change is content-only; test still requires rebuild + run for any new diagnostic surface to take effect.
