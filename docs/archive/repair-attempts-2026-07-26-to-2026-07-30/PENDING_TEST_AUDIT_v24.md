# Pending Test Audit v24

- tests: docs/PENDING_TESTS_v24.md
- commit: docs/PENDING_COMMIT_v24.md
- verdict: ALL_KEEP
- verifier: cron (file-only role #6, six-role-pipeline)
- timestamp: 2026-07-27T11:30:00Z

## Broken-pattern audit

- [ ] No from-x-import-y patch propagation bugs: PASS (script is standalone; no patch chain)
- [ ] No test-bug-in-itself (asserts against wrong fixture): PASS (Part A tests assert against actual file contents via `read_file`/`search_files`)
- [ ] No source-incomplete-relative-to-test: PASS (script is the only artifact produced; no source-code changes)
- [ ] No missing test isolation fixture: PASS (Part A tests are file-content checks; no shared state)
- [ ] No AsyncMock on sync function (or vice versa): N/A (Python script, no async)

## Per-test verdict

### Part A — cron-verifiable static tests (13/13 PASS)

| Test | Status | Evidence |
|------|--------|----------|
| A1 — file present | PASS | `search_files` returns 1 match |
| A2 — file non-empty + has main/compute_stats/emit_stats | PASS | `read_file` shows 166 lines, 6212 bytes; `def main` at line 122 confirmed via `search_files` |
| A3 — shebang line | PASS | first line is `#!/usr/bin/env python3` |
| A4 — minimal imports (stdlib + PIL + numpy only) | PASS | lines 47-51: argparse, glob, os, sys, typing; lines 53-55: numpy + PIL inside try block |
| A5 — channel patterns | PASS | line 63: `("display", "display_frame*.png")` confirmed; docstring lines 19-22 enumerate all 4 channels |
| A6 — ImportError handling | PASS | line 56: `except ImportError as e:` followed by helpful error + `sys.exit(1)` |
| A7 — missing data dir | PASS | line 137 (approx): `os.path.isdir(data_dir)` check + exit 1 |
| A8 — empty data dir | PASS | `if not dump_dirs:` branch with "nothing to inspect" message |
| A9 — --data-dir arg | PASS | `add_argument("--data-dir", default=os.path.dirname(os.path.abspath(__file__)))` |
| A10 — per-channel output format | PASS | line 100: `f"    {name}: mean={mean:6.2f} std={std:6.2f} unique={unique:3d} sat255={frac_high * 100.0:5.1f}% sat0={frac_low * 100.0:5.1f}%"` |
| A11 — clamp detection | PASS | line 105: `if frac_high > 0.5 and unique > 50:` |
| A12 — companion script to validator | PASS | header docstring lines 11-16 document the dual-role relationship |
| A13 — transient C++ draft neutralized | PASS | `dump_pixelstats.cpp` is 0 bytes (confirmed via `read_file`); parent should remove the placeholder via `rm` |

### Part B — parent-driven runtime tests (5 PENDING)

| Test | Status | Notes |
|------|--------|-------|
| B1 — runs on stale dumps/ | PENDING | parent-driven; cron terminal blocked |
| B2 — empty data dir graceful | PENDING | parent-driven |
| B3 — --data-dir flag works | PENDING | parent-driven |
| B4 — clamp detection fires | PENDING | parent-driven; depends on B1 first |
| B5 — exit code 0 on no dumps | PENDING | parent-driven |

### Part C — cleanup (1 PENDING)

| Task | Status | Notes |
|------|--------|-------|
| C1 — remove transient C++ placeholder | PENDING | parent-driven; trivial (`rm` 0-byte file) |

## Verdict rationale

ALL_KEEP because:
1. All 13 Part A cron-verifiable static tests pass via file-content inspection.
2. The 5 Part B runtime tests are genuinely parent-driven (terminal blocked in cron for 14+ ticks).
3. The 1 Part C cleanup task is trivial and explicitly flagged for parent attention.
4. No broken-pattern audit item fires.
5. The script does not modify any source code, marker, or pipeline state; it is fully reversible.

## Single-head caveat

This audit is generated by the same cron session that wrote the tests and the script. Per `software-development-practices §6-role pipeline on a single-profile host: manage the freshness caveat explicitly`, this ALL_KEEP is more like a self-check than an independent audit. The script is mechanically simple (Python stdlib + PIL + numpy, 166 lines, well-scoped, defensive error handling, well-documented dual-role with the existing validator) so the single-head caveat does not block ALL_KEEP.

## What's next

The v24 cycle is complete. Suggested parent triage sequence:

1. (cleanup) `rm Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/dump_pixelstats.cpp` — remove the 0-byte placeholder
2. (fast first-look, ~5s) `python3 Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/dump_pixelstats.py` — get per-channel stats for the stale `dumps/20260727_000706`–`000708` group
3. (interpret) Read the output:
   - If gi_raw has std=0 + sat255=100% → confirm dump-encoder clamp; investigate `FImageDump::DumpToPNG` per-channel normalization
   - If gi_raw has std>0 but display/spatial/denoised have std=0 → confirm downstream-pass-overwrite issue
   - If gi_raw has std>0 AND display has std>0 → render chain has signal; run the FIXED `run_rgi_diagnostic.sh` for full per-mode evidence
4. (full evidence, ~10min) `bash Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/run_rgi_diagnostic.sh` — produces per-mode dumps + stderr + rgi_evidence.txt
5. (paste back) Paste `rgi_evidence.txt` to cron; cron routes to v22 (v21a binding-layout-split fix) or v21b..v21i sub-plan based on evidence shape

The v22 PICK item status is UNCHANGED — still `[ ]` and gated on parent running the FIXED script. v24 adds the dump_pixelstats.py fast first-look capability but does not advance v22.