# Pending Plan Review v22

- plan: docs/PENDING_PLAN_v22.md
- verdict: KEEP
- reviewer: cron (file-only role #2, six-role-pipeline)
- timestamp: 2026-07-27T12:00:00Z

## Design soundness

The v22 plan correctly identifies v21a as the next-cycle action item from PENDING_PICK.md line 143. The plan's design is sound because:

1. **Hypothesis is well-grounded**: nvrhi-deferred-barrier-ordering is a documented nvrhi pattern (the v21 plan references `gpu-rendering-bisect-debug/references/nvrhi-deferred-barrier-ordering.md`). The symptom (7x `DeviceManager.cpp:52` warning per stale run + gi_raw=0,0,0) matches the pattern's described symptoms.
2. **The fix is mechanical and reversible**: split one binding set into two (SRV + UAV), add a new DispatchRays overload, no HLSL changes, no binding index changes.
3. **Risk A (multi-binding-set RT support) is verified**: FRayTracingPipeline.cpp:304-332 already has a 7-arg overload with `State.addBindingSet(DescriptorTable)` — the new 6-arg overload is a clean addition that follows the same pattern.
4. **The plan is well-bounded**: 4 files, +50/-25 lines, no scope creep into TestReSTIR_GI_Temporal.cpp, GIPathTracing.hlsl, or any of the other test files.

The plan correctly identifies all 6 verification steps and stages them as parent-driven (because the cron's terminal access is blocked by tirith). The plan does NOT claim success; it explicitly defers to parent verification.

## Plan completeness

The plan correctly enumerates the 6 risks from v21 (A-F) and provides concrete mitigations:
- A: verified by code reading (FRayTracingPipeline.cpp:304-332)
- B: HLSL register-to-binding-set mapping preserved exactly
- C: shader table is separate from binding sets
- D: 2 sets << 4 per pipeline
- E: no `(uintptr_t)` or `reinterpret_cast` patterns introduced
- F: single-head caveat acknowledged

Missing items (non-blocking):
- The plan does not enumerate the cascade-aware -Werror fix recipe from `software-development-practices §werror-cascade-fix-recipe.md` — but Risk E covers the specific pattern. If the build breaks on a different file, the parent should follow the recipe.
- The plan does not stage a fallback to v21b..v21i if v21a is wrong. The v21 plan has them pre-staged; v22 is one-shot for v21a only.

## Feedback for planner (FIX only)

None. Plan is sound and ready for impler.

## Cycle gating rationale

The v22 PICK item is labeled "parent-driven; ONLY fires after parent runs the FIXED `run_rgi_diagnostic.sh` AND `rgi_evidence.txt` confirms hypothesis #1." The v21 plan was KEEP'd by plan-review v21 (implied by PICK line 141). The cron's user instruction for THIS cycle is "continue cycles from PENDING_PICK through planner, plan-criticer, impler, reviewer, tester, and testing-verifier, then repeat any failed/fix cycle or next debugging item until the acceptance criteria are actually met" + "autonomous until complete" + "do not silently stop." This instruction explicitly authorizes v22 firing despite the parent-driven label, with the caveat that final verification is parent-driven.

The v28 heartbeat's "revoked without evidence" rejection was correct in isolation but does not apply when the cron's user instruction explicitly authorizes the cycle. The plan is sound; the cycle proceeds.

## Single-head caveat

This plan-review is generated by the same cron session that wrote the plan. Per `software-development-practices §6-role pipeline on a single-profile host`, this KEEP is more like a self-check than an independent review. The plan is mechanically sound (well-grounded hypothesis, reversible patch, 6 risks enumerated, 6 verification steps staged) so the single-head caveat does not block KEEP.
