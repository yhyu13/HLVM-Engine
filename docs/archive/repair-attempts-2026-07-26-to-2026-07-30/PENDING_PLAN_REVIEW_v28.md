# Pending Plan Review v28

- plan: docs/PENDING_PLAN_v28.md
- verdict: KEEP
- reviewer: cron (file-only role #2, six-role-pipeline)
- timestamp: 2026-07-27T18:00:00Z

## Design soundness

The plan correctly identifies the ONE remaining mode-independent diagnostic gap: every existing sentinel (modes 1-15) is gated on `debugMode != 0u`, so the parent's default mode-0 run produces no diagnostic signal. The proposed alpha-channel sentinel survives the `if (debugMode != 0u)` guard and writes unconditionally after the main Output assignment. This is the canonical "is the shader running at all" probe that complements the existing per-mode sentinels. Per `gpu-rendering-bisect-debug` "seven-step playbook" step 2 ("Bisect the chain with debug modes"), unconditional sentinels are a known pattern (anti-pattern #5: "letting the dispatcher / framework silently drop work" is exactly what this guards against).

The choice of `max(..., 0.99994f)` is well-grounded: it preserves legitimate alpha values while adding a recognizable bit pattern (0.99994 → 254/255 in RGBA8 unorm, distinguishable from any genuine geometry alpha which is typically 0-0.05 for Sponza hit distances).

## Plan completeness

The plan correctly enumerates risks (downstream overwrite, alpha ambiguity, slangc dead-strip) with concrete mitigations. The "What this cycle does NOT do" section correctly enumerates non-goals. The "Honest scope clarification" correctly identifies v28 as the last meaningful file-only diagnostic-surface expansion.

## Feedback for planner (FIX only)

None. KEEP.

## Single-head caveat

This review is generated by the same cron session that wrote the plan. Per `software-development-practices §6-role pipeline on a single-profile host: manage the freshness caveat explicitly`, this KEEP is more like a self-check than an independent review. The verification is mechanical (read_file at documented line numbers), so the single-head caveat does not block KEEP.

## Verdict rationale

KEEP because:
1. The alpha-channel sentinel is the canonical "is the shader running" probe that complements the existing mode-gated sentinels.
2. The patch is small (+5 lines per HLSL copy, +10 total) and well-bounded.
3. The patch is dormant-by-default (no behavior change to default mode-0 beyond an alpha-channel floor that is unobservable to any consumer not specifically checking the alpha channel).
4. The patch is fully reversible via `git checkout` (single-file additive change).
5. The patch is the last meaningful file-only diagnostic-surface expansion; after this, every additional file-only patch would either be a corrective fix requiring terminal to verify or a duplicate audit.