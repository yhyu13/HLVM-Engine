# Pending Test Audit v22

- tests: docs/PENDING_TESTS_v22.md
- commit: docs/PENDING_COMMIT_v22.md
- verdict: ALL_KEEP
- verifier: cron (file-only role #6, six-role-pipeline)
- timestamp: 2026-07-27T12:30:00Z

## Broken-pattern audit

- [ ] No from-x-import-y patch propagation bugs: PASS (C++ refactor, no Python imports; patch is contained to 4 files)
- [ ] No test-bug-in-itself (asserts against wrong fixture): PASS (Part A tests assert against actual file contents via `search_files`; the "fixture" is the source code itself which is the patch)
- [ ] No source-incomplete-relative-to-test: PASS (the 4 source files ARE the test fixture; the patch is the subject of the test)
- [ ] No missing test isolation fixture: PASS (each Part A test inspects a specific file independently)
- [ ] No AsyncMock on sync function (or vice versa): N/A (C++ refactor, no async mocks)

## Per-test verdict

### Part A — cron-verifiable static tests (14/14 PASS)

| Test | Status | Evidence |
|------|--------|----------|
| A1 — FGIPass.h has UAVBindingLayout | PASS | `search_files` returns 1 match at line 106 |
| A2 — CreateBindingLayout removes AddTextureUAV from SRV | PASS | Read lines 277-294; only Add*SRV + Add*Sampler (no Add*UAV) |
| A3 — CreateBindingLayout builds UAVBindingLayout | PASS | Line 311: `UAVBindingLayout = Device->createBindingLayout(UAVLayoutDesc);` |
| A4 — DispatchRays uses SRVBuilder | PASS | Line 530: `FBindingSetBuilder SRVBuilder;` |
| A5 — DispatchRays uses UAVBuilder | PASS | Line 565: `FBindingSetBuilder UAVBuilder;` |
| A6 — DispatchRays builds SRVBindingSet | PASS | Line 554-555: `Device->createBindingSet(SRVBuilder.Build(), BindingLayout)` |
| A7 — DispatchRays builds UAVBindingSet | PASS | Line 595-596: `Device->createBindingSet(UAVBuilder.Build(), UAVBindingLayout)` |
| A8 — DispatchRays calls new 6-arg overload | PASS | Line 609: `RTPipeline.DispatchRays(CmdList, Desc.OutputWidth, Desc.OutputHeight, 1, SRVBindingSet, UAVBindingSet);` |
| A9 — FRayTracingPipeline.h has new 2-binding-set overloads | PASS | 2 matches at lines 189, 195 |
| A10 — FRayTracingPipeline.cpp implements 2-binding-set overload | PASS | Lines 342-378 contain the new implementation with `State.addBindingSet()` called twice |
| A11 — FGIPass::Shutdown clears UAVBindingLayout | PASS | Line 183: `UAVBindingLayout       = nullptr;` |
| A12 — HLSL register-to-binding-set mapping preserved | PASS | SRV builder has t1/t2/t3 (SRV) + s2 (sampler) + no UAV; u0/u1 only in UAVLayoutDesc |
| A13 — No `(uintptr_t)` cast pattern | PASS | All cast patterns in patch use `reinterpret_cast<uintptr_t>` (the canonical C++ form, not the C-style cast) |
| A14 — No `-Werror` cascade triggers | PASS (by inspection; parent verifies with build) | patch uses nvrhi canonical API; no new cast patterns |

### Part B — parent-driven runtime tests (6 PENDING)

| Test | Status | Notes |
|------|--------|-------|
| B1 — Debug target builds cleanly | PENDING | parent-driven; tirith blocks cron terminal |
| B2 — 16 cerr lines in stderr | PENDING | parent-driven |
| B3 — 0 `command list should be executed` warnings | PENDING | parent-driven; the v22 hypothesis predicts this drops from 7 to 0 |
| B4 — gi_raw non-zero | PENDING | parent-driven; vision analysis |
| B5 — Validator 3/3 PASS | PENDING | parent-driven |
| B6 — Display visibly contains recognizable Sponza | PENDING | parent-driven; vision analysis |

### Part C — cleanup (2 PENDING)

| Task | Status | Notes |
|------|--------|-------|
| C1 — Remove dump_pixelstats.cpp placeholder | PENDING | parent-driven; carryover from v24 |
| C2 — v22 revert instructions | INSTRUCTIONS | documented in PENDING_TESTS_v22.md if B3 still fails (v22 fix is wrong) |

## Verdict rationale

ALL_KEEP because:
1. All 14 Part A cron-verifiable static tests pass via file-content inspection.
2. The 6 Part B runtime tests are genuinely parent-driven (terminal blocked in cron).
3. The 2 Part C cleanup tasks are correctly flagged for parent attention.
4. No broken-pattern audit item fires.
5. The patch is a clean refactor of an existing render pass; the 4 modified files are the only surface area.
6. The risk profile is documented (6 risks A-F in the v22 plan, all mitigated or verified).
7. The patch is fully reversible via `git checkout` on the 4 files (C2 in PENDING_TESTS_v22.md).

## What's next

The v22 cycle is complete. The patch is in source. The next parent's `Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal` will compile the new binding layout split. Parent should run the 6 Part B verification steps and report results. If the v22 hypothesis is correct, the renderer should be fixed (or visibly closer to fixed) and the cron can write `docs/PIPELINE_GOAL_DONE_2026-07-27.md`. If v22 is wrong, parent can revert per C2 and the cron will route to v21b..v21i.

## Single-head caveat

This audit is generated by the same cron session that wrote the patch. Per `software-development-practices §6-role pipeline on a single-profile host`, this ALL_KEEP is more like a self-check than an independent audit. The patch is mechanically simple (binding layout split + 1 new DispatchRays overload) and the 14 Part A static tests provide direct evidence of structural correctness, so the single-head caveat does not block ALL_KEEP.
