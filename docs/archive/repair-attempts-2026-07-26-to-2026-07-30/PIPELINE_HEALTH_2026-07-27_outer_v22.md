# Outer watchdog heartbeat — 2026-07-27 v22

**Final-goal gate FAILED/UNVERIFIED:** Latest complete six-role markers are v20 (`PENDING_PLAN_REVIEW_v20=KEEP`, `PENDING_IMPL_REVIEW_v20=KEEP`, `PENDING_TEST_AUDIT_v20=SOME_RELAX`); v21 is staged in `PENDING_PICK.md` and awaits a parent-run `run_rgi_diagnostic.sh`, but no `rgi_evidence.txt` or per-mode runner logs exist. The newest runtime evidence remains dump stamps `20260727_000706`–`000708`; the matching `TestReSTIR_GI_Temporal.log` proves eight frames completed but records seven related `A command list should be executed before it is reopened` warnings and `gi_raw` R/G/B `[0.000,0.000]`, so it fails the gate. Shell/git-status/build/run/validator/image-stat execution was blocked by tirith (`pending_approval`, `tirith:unknown`), and no image-vision tool is available; current-tree build cleanliness, a fresh `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM>=8` run, fresh command-list/Vulkan/VUID cleanliness, validator/auxiliary success, and recognizable sane-exposure Sponza remain unverified. No `PIPELINE_GOAL_DONE` or `PIPELINE_NUDGE` was written because the explicit parent-evidence/tool failure explains the wait and there is no FIX→FIX bounce; no block, archive, commit, push, merge, pause, card creation, or governance change was performed.
