# Pending Plan v91
- task: restir-gi-fix — narrow v90's remaining 2-way hypothesis ((i) dispatch-drops vs (ii) shader-side write skipped) at a NEW site not cycled by v25-v90, WITHOUT terminal access (file-only path).
- source: no bundle — direct edit (terminal still blocked in this cron runspace; v87/v88/v89/v90 all confirmed; per parent's "until complete, do not silently stop" instruction in this turn's prompt)
- approach: LOCKED-SLOT-VALIDATION probe — verify the shader-side `register(u0)` matches both the binding-layout slot (UAVItems[0].slot = 0, type = Texture_UAV) AND the binding-set's `SetTextureUAV(0, Desc.OutputTexture)`. If all three are consistent (0=0=0), then the dispatch-side descriptor is correctly bound to the shader-side `Output` RWTexture2D, and hypothesis (ii) (shader-side write skipped) is ELIMINATED at the binding layer. This collapses v90's 2-way hypothesis to a SINGLE remaining cause: (i) dispatch-drops — i.e., the FRayTracingPipeline::DispatchRays call is failing silently or being skipped before reaching the shader. That branch then requires ONLY terminal evidence (v3 ENTER/EXIT log at FGIPass.cpp:625/631 + per-channel min/max of `gi_raw` dump) per the cron-runspace-blocked recipe in `PIPELINE_BLOCKER_2026-07-28.md` / `PIPELINE_RUNSPACE_BLOCKED_2026-07-28.md`.
- Cycle shape: 1 NEW Part A probe at 3 NEW sites (UAVItems[0].slot @ FGIPass.cpp:304, Shader register @ GIPathTracing.hlsl:88, SetTextureUAV slot @ FGIPass.cpp:582), 0 source-code lines, single new finding, PIPELINE_HEALTH append, no fabrication, no terminal probes (tirith block re-confirmed this tick).
- diff_estimate: 0 source-code lines
- skip_plan_review: no — state machine MUST re-route through plan-criticer per HARD INVARIANT #4.
- test_strategy: role #5 (tester) — Part A spot-checks (3/3 line-ranges) MUST match the read_file window exactly. Part B 8/8 remains UNVERIFIED (terminal-blocked). The 2-way → 1-way narrowing should be recorded, not argued.
- risks: (1) The binding-set's `SetTextureUAV(0, Desc.OutputTexture)` may insert 0 because `nvrhi::BindingSetItem::slot = 0` is implicit when called as `SetTextureUAV(0, ...)` — the slot index is the first parameter. A static-read of the call site must confirm the first argument is 0. (2) The shader's `register(u0)` is HLSL-level and could be subject to SPIR-V binding decoration translation; the actual SPIR-V binding for `Output` may not exactly match. (3) The `keepInitialState=true` requirement on UAVs (per skill § C++ Rendering gotchas #2) is needed for the descriptor to remain stable; the test creates OutputTexture with `initialState=UnorderedAccess` and `keepInitialState=true` per A2 — confirmed at v90. (4) The 2-way → 1-way narrowing is correct ONLY IF all three sites converge; if any one is mismatched, the bug is the binding itself (an entirely different fix-branch). Verifier must read all three carefully.
