# Pending Plan v90
- task: restir-gi-fix — produce ONE fresh Part A probe at a NEW diagnostic site not cycled by v25-v89, resolving v89's three-candidate downstream surface question WITHOUT terminal access (file-only path).
- source: no bundle — direct edit (parent terminal access still blocked; pipeline in runspace-blocked posture per v87/v88/v89 + PIPELINE_RUNSPACE_BLOCKED_2026-07-28.md)
- approach: LOCKED-NARROWING probe — verify the dump-read-handle IS the dispatch-write-handle by static-read of TestReSTIR_GI_Temporal.cpp lines 410-450 + 935-960 + 1620-1660. v89 finding named 3 candidate downstream surfaces ((i) dispatch drops, (ii) shader-side write skipped, (iii) dumper-side mismatch — debugger-side). A static-read of the dump's call site + the dispatch's `Desc.OutputTexture` propagation closes hypothesis (iii): if both copies of the handle trace back to the same `OutputTexture` member created via `CreateTexture2D` at line 937, then the dump reads the SAME texture the dispatch writes. The bug then MUST be (i) dispatch-drops OR (ii) shader-side write skipped — both of which require terminal evidence per the v87/v89 comment at line 1695 to disambiguate. Cycle shape: 1 NEW Part A probe (dumper-side handle chain at lines 410-450 + 935-960 + 1620-1660), narrow v89's 3-way hypothesis to 2-way, NO source-code modifications, fresh single finding, PIPELINE_HEALTH append, no fabrication, no terminal calls (block confirmed again at v90).
- diff_estimate: 0 source-code lines
- skip_plan_review: no — per-cycle state machine MUST re-route through plan-criticer per HARD INVARIANT #4.
- test_strategy: role #5 (tester) — Part A spot-checks (3/3 line-ranges) MUST match the read_file window exactly. Part B 8/8 remains UNVERIFIED (terminal-blocked). The 2-way narrowing should be recorded, not argued.
- risks: (1) Static-read of `OutputTexture` member may miss a *re-assignment* between dispatch and dump if `OutputTexture = ...` appears anywhere after line 634 (FGIPass.cpp sets a local `OutputTexture = Desc.OutputTexture` — that's the GI namespace's member, not the test's; verify at v90 if it's the same member). (2) The dumper's `Cmd->copyTexture(Staging, Slice, Texture, Slice)` at line 1712 passes the **handle** as a parameter; if the API takes a stale handle by value, the dumper still reads from the same texture as the dispatch. (3) The v22 split at FGIPass.cpp:621-632 explicitly logs the dispatch return — v3 ENTER/EXIT logs + per-channel min/max of gi_raw are the 10-second diagnostic; this plan's file-only narrowing is *a complement* to that terminal probe, not a replacement.
