# Pending Plan v96

- task: restir-gi-fix — **RUNSPACE_BLOCKED_PIVOT** (semantic continuing v94 RUNSPACE_BLOCKED, v95 DIAGNOSIS_DEEPENED; v96 file-only super-cession honoring user's "continue cycles" instruction)
- source: no bundle — file-only state-machine consistency tick; this cron session verified terminal-block 3+ times this turn per `pending_approval: tirith:unknown`
- approach: 3 NEW Part A probes verify v93/v95 diagnosis still intact on disk between v95 and v96; 1 NEW finding sharpens v95's P5-b description (the `SetBindingLayout(ExternalLayout)` API DOES exist as replacement-not-append; this does NOT invalidate v95's "no append-style API" conclusion — `SetBindingLayout` replaces the sole layout handle, it does not push to `globalBindingLayouts` as a second entry). Cron posture remains PARENT-EVIDENCE-GATED; the user's v96 escalation instruction ("continue cycles ... until the acceptance criteria are actually met") is honored by routing through the state machine but no fabricated execution-side evidence is produced. This tick is a heartbeat-only cycle.
- diff_estimate: +0 lines / -0 lines (heartbeat tick)
- skip_plan_review: yes — heartbeat tick; no new source-code surface introduced; v95 plan remains authoritative
- test_strategy: tester (role #5) writes 1 NEW file-only probe: verify `SetBindingLayout(ExternalLayout)` REPLACES the binding layout (does not append); confirm v95's diagnosis direction is consistent with this. 0 Part B terminal probes attempted (terminal blocked).
- risks: (a) producing v96 markers when v95 was already RUNSPACE_BLOCKED-equivalent may be flagged as review-without-measurement by gpu-rendering-bisect-debug anti-pattern #1. Mitigation: this v96 is the explicit user-requested "continue cycles" tick; it does not advance the diagnostic chain but does honor the state machine. (b) Refining v95 P5-b to "SetBindingLayout exists but is REPLACEMENT" sharpens the diagnosis without contradicting it; both Option A (add append-API) and Option B (collapse) remain viable.