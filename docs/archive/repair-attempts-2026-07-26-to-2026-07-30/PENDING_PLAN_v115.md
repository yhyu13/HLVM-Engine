# Pending Plan v115
- task: restir-gi-fix-runtime-verification-v115 — execute and audit the v114 split-layout repair against the complete GPU acceptance contract
- source: current working-tree source from `PENDING_COMMIT_v114.md`; no bundle and no patch application
- approach: Treat v114 as the implementation under test and avoid further source edits until fresh execution identifies a specific failure. Build `TestReSTIR_GI_Temporal`, snapshot the pre-run dump/log frontier, run from a clean command-list state with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`, then evaluate only artifacts newer than that frontier: process exit, fresh log error scan, newest coherent frame-8 dump group, the four structural checks, and display-image geometry/exposure. If any gate fails, record the exact first failing evidence and let a later impler make one root-cause-directed change; do not apply the now-stale v101 patch or run its mutating helper because v114 already supersedes it.
- diff_estimate: +0 / -0 source lines for the verification-first path; any diagnosed fix must be documented by the impler as a plan deviation and kept minimal
- skip_plan_review: no
- test_strategy: Role #5 must run `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal`; record dump/log mtimes before launch; run the target with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; require no `command list.*already open`, Vulkan `ERROR`, or `VUID-` in the newly generated log region; isolate only the newest timestamped frame-8 group rather than allowing the validator to mix historical groups; run the existing validator semantics against that group; compute display RGB mean/std, 4x4 cell variance, black/saturated fractions, and alpha sentinel; inspect the fresh display itself for recognizable arches/columns/plaza and sane exposure. Also run the closest relevant existing check if the target exposes one through `Build.sh --Test` without invoking parallel ctest.
- risks: Terminal invocation in this planner tick was denied as `pending_approval: tirith:unknown`, despite terminal-enabled cron configuration, so no build/run claim exists yet. The existing validator glob loads every historical `*frame8.png`, which violates the newest-group-only acceptance contract unless the tester stages or explicitly filters the fresh group. `fresh-evidence-scan-v93.sh` is stale and mutating: its precondition expects v114 markers to be absent and it applies the incomplete v101 patch, so it must not be used on current source. No available image-analysis tool can read PNGs in this runspace; numeric structural evidence is not a substitute for the required visual gate.

## Expected evidence and routing
1. Build failure: preserve the first compiler/linker error and route to impler for a cascade-aware minimal fix.
2. Run/log failure: preserve exit code and fresh log excerpt; bisect command-list/Vulkan state before changing shaders.
3. Structurally bad image: use existing GI debug modes in increasing invasiveness (GBuffer → direct/GI → hardware IDs/barycentrics → sentinel boundary), one variable per experiment.
4. Structural checks pass but geometry/exposure remains visually wrong: verdict remains incomplete; do not accept the scalar validator.
5. Every gate passes on fresh artifacts: reviewer/tester/verifier may close v115 and write goal-done evidence; no production edit is required.
