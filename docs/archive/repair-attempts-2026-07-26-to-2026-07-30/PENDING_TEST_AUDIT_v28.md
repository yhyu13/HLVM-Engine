# Pending Test Audit v28

- tests: docs/PENDING_TESTS_v28.md
- commit: docs/PENDING_COMMIT_v28.md
- verdict: ALL_KEEP
- verifier: cron (file-only role #6, six-role-pipeline)
- timestamp: 2026-07-27T18:00:00Z

## Broken-pattern audit
- [ ] No from-x-import-y patch propagation bugs: PASS (HLSL-only; no import chain)
- [ ] No test-bug-in-itself (asserts against wrong fixture): PASS (Part A tests assert against actual HLSL line numbers via read_file; every match is at the expected site)
- [ ] No source-incomplete-relative-to-test: PASS (patch is the only source change; tests match it)
- [ ] No missing test isolation fixture: PASS (Part A tests are independent file-content checks; Part B is parent-driven runtime check)
- [ ] No AsyncMock on sync function (or vice versa): N/A (HLSL is not async)

## Per-test verdict

### Part A — cron-verifiable static tests (5/5 PASS)

| Test | Status | Evidence |
|------|--------|----------|
| A1 — v28 sentinel in Private master | PASS | `Output[pixel].w = max(Output[pixel].w, 0.99994f);` confirmed at line 692 via read_file |
| A2 — v28 sentinel in data-dir copy | PASS | identical sentinel at line 692 via read_file |
| A3 — v28 comment block | PASS | 10-line comment explaining sentinel purpose + 254/255 RGBA8 prediction |
| A4 — v28 byte-identical sync | PASS | diff shape shows 0 meaningful differences; both HLSL copies byte-identical at sentinel site |
| A5 — v28 no prior-case regression | PASS | both copies still have case 1u-15u + default at lines 579-677 |

### Part B — parent-driven runtime tests (1 PENDING)

| Test | Status | Notes |
|------|--------|-------|
| B1 — alpha-channel inspection | PENDING | parent-driven; vision-analyze display_frame8.png alpha after rebuild |

## Verdict rationale

ALL_KEEP because:
1. All 5 Part A cron-verifiable static tests pass via read_file at documented line numbers this tick.
2. The v28 sentinel is byte-identical between Private master and data-dir copy (load-bearing pair per v16 understanding).
3. The patch does NOT regress any prior case (case 1u-15u + default still present).
4. No broken-pattern audit item fires.
5. The patch is dormant-by-default (alpha channel is unobservable to any consumer not specifically inspecting it).
6. The patch is fully reversible via `git checkout` on the 2 HLSL files.

## Single-head caveat

This audit is generated by the same cron session. Per `software-development-practices §6-role pipeline on a single-profile host`, this ALL_KEEP is more like a self-check than an independent audit. The verification is mechanical (read_file at documented line numbers; diff between HLSL copies), so the single-head caveat does not block ALL_KEEP.

## What's next

The v28 cycle is complete. The pipeline remains gated on parent rebuilding and running the test. The new alpha-channel alive-sentinel gives parent a definitive "yes the dispatch body ran" signal on the next default-mode rebuild run, independent of whether they remember to set `HLVM_PT_DEBUG_MODE=6`. Suggested parent triage sequence:

1. (verify pre-conditions) `cat docs/PIPELINE_HEALTH_2026-07-27.md | tail -100` — confirm v28 audit ALL_KEEP
2. (build) `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal --Test`
3. (run diagnostic) `bash Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/run_rgi_diagnostic.sh`
4. (inspect) `cat rgi_evidence.txt`
5. (alpha-channel inspection) On default mode, examine the alpha channel of `display_frame8.png`:
   - If alpha saturated to 254-255 across all pixels → dispatch body reached line 682+ → bug is downstream (lighting math, payload, accumulate, denoise). Cron routes to v29 = investigate failing downstream stage.
   - If alpha uniformly 0 → dispatch body never executed → bug is upstream (binding layout, descriptor mismatch, dispatch setup). Cron routes to v29 = investigate nvrhi dispatch.
6. (vision) Vision-analyze `display_frame8.png` for recognizable non-uniform Sponza geometry with sane exposure.

If the structural terminal block persists indefinitely, the pipeline stays at this heartbeat. Per the cron's "Never silently exit" hard rule, every tick must write SOMETHING to PIPELINE_HEALTH. v28 extends the diagnostic surface with the last meaningful file-only patch; after v28, every additional file-only patch would either be a corrective fix that requires terminal to verify or a duplicate audit of unchanged source.