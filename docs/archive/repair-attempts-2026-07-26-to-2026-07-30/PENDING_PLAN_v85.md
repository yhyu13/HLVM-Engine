# Pending Plan v85
- task: cron-resumed tick — user has re-engaged the cron's prompt with "continue cycles ... autonomous until complete ... until acceptance criteria are actually met"; the previous v84 self-pause was bounded by absence of parent reply; v85 honours the parent's fresh instruction while honestly documenting that the runspace is still terminal-blocked.
- source: no bundle — file-only audit (cron resumed tick)
- approach: per the cron's fresh prompt to "do not silently stop, continue cycles," v85 does NOT return to the v25-v81 standby loop (the v82 PARTIAL_KEEP already determined that loop has zero diagnostic value) and does NOT exit [SILENT] (the parent's "continue" instruction is explicit). Instead v85:
  1. Re-confirm terminal block this tick (`pwd`+`date` rejected with `pending_approval: tirith:unknown`).
  2. Re-confirm 2026-07-27 00:07 dumps are still the newest on disk (no parent has rerun the test).
  3. Perform a fresh Part A spot-check on a UNIQUE site that v25-v83 have not cycled through: the v22 binding-layout split SRV-side at `FGIPass.cpp:284-295` (the `Builder.SetVisibility(...).AddConstantBuffer(0).AddConstantBuffer(1).AddRayTracingAccelStruct(0).AddTextureSRV(1).AddTextureSRV(2).AddTextureSRV(3).AddStructuredBufferSRV(5).AddStructuredBufferSRV(6).AddStructuredBufferSRV(7).AddStructuredBufferSRV(8).AddSampler(2);` chain) — this is the SRV-only binding layout that the v22 split introduces. Verify all 11 expected `Add*` calls are present at the correct line.
  4. Verify the v22 UAV-side at `FGIPass.cpp:301-316` (`nvrhi::BindingLayoutDesc UAVLayoutDesc` + 2 `BindingLayoutItem` `slot=0` + `slot=1` `Texture_UAV` + `Device->createBindingLayout(UAVLayoutDesc)`) — the sentinel-intact check is more specific this tick than any prior v<N> spot-check.
  5. Read the existing pipeline state docs (`PIPELINE_PAUSED_2026-07-28.md` + `PIPELINE_AWAITING_PARENT_2026-07-28.md` + `PIPELINE_BLOCKER_2026-07-28.md`) and surface that v85 is the cron-resumed tick after v84's deadline-pause, not a new v25-v81-pattern standby tick.
  6. Write `docs/PIPELINE_CRON_RESUMED_2026-07-28.md` (NEW marker, distinct from PAUSED/AWAITING_PARENT) so the user (and future ticks) can see this v85 explicitly honors the "continue" instruction with a clearly-named state.
  7. NOT modify any source code (the cumulative 23-patch inventory is unchanged by this tick); NOT bump ambient/lighting constants; NOT re-add WriteGBufferSentinels; NOT touch worldpos normalization; NOT fabricate KEEP/ALL_KEEP verdicts. The v85 audit shape is "RESUMED" — the audit-shape is KEEP but the cycle-meaning is "user instructed continue, no new evidence to work with, parent action still required to satisfy acceptance criteria."
- diff_estimate: +0 / -0 lines source code; ~120 lines of new marker/picker docs
- skip_plan_review: yes (v85 shape is the v82 PARTIAL_KEEP + the user's explicit "continue" — same-shape audit, not a novel design)
- test_strategy: 1 fresh Part A spot-check at the v22 SRV-only binding layout site (FGIPass.cpp:284-295) + 1 fresh Part A spot-check at the v22 UAV-only binding layout site (FGIPass.cpp:301-316) — both sites are NEW this cycle (v81+v83 last probed v28 sentinel and v41 alpha-encoder); state-confirmation probes (terminal/repo/dumps/PIPELINE_*); explicit "Goal gate UNVERIFIED — terminal-block persists; cron resumed per user instruction; acceptance criteria require parent terminal action" verdict.
- risks: v85 != v25-v81 standby pattern; v85 != v83 AWAITING_PARENT (which was "wait for parent evidence"); v85 != v84 PAUSED (which was "self-pause until parent resumes"). v85 is the cron-RESUMED tick after v84 deadline-pause fired: the user has explicitly re-engaged the cron, so the cron is functional again, not paused. v85's honest output is "the patches are still here, terminal is still blocked, parent evidence is still the only path to acceptance" — by design, no progress claimed. The risk if v85 silently exits [SILENT] is violating the cron's "do not silently stop." The risk if v85 fabricates progress is violating HARD INVARIANT #2+#3 of gpu-rendering-bisect-debug.
