# Pipeline Health — 2026-07-30 scheduled tick 105

- Authoritative `docs/PENDING_PICK.md` line 11 task unchanged from ticks 17–104: "Continue bisecting GBuffer SRV binding in the GI shader (per docs/DIAGNOSTIC_2026-07-30.md 'Recommended next step'). One variable per experiment." Top item is not `[x]`. No v125+ plan/commit/tests/audit markers, no `PIPELINE_GOAL_DONE_*.md`, no `PIPELINE_NUDGE_*.md`, no `RUNSPACE_*.md` updates exist. PENDING_PICK remains the authoritative queue.
- Read-only terminal probe (`pwd`, `ls docs/`, file-existence sanity on `Engine/Source/Runtime/Binary/Debug/TestReSTIR_GI_Temporal`) rejected before launch with `status=pending_approval`, `exit_code=-1`, `pattern_key=tirith:unknown` — identical to every tick 17–104. No command executed; no fresh build, run, log, dump, validator, structural statistic, or visual Sponza inspection produced in this turn. The cron prompt asserts terminal access but the runspace's tirith gate blocks every probe before launch.
- **NEW file-only evidence this tick (diagnostic delta):** the v101 patch proposed in `docs/archive/repair-attempts-2026-07-26-to-2026-07-30/restir-gi-fix-v101.patch` is **fully applied** to the working tree:
  - `Engine/Source/Runtime/Public/Renderer/RayTracing/FRayTracingPipeline.h:8` includes `Core/Container/ContainerDefinition.h`.
  - `FRayTracingPipeline.h:123` declares `void AddBindingLayout(nvrhi::BindingLayoutHandle InLayout);`.
  - `FRayTracingPipeline.h:236` holds `TVector<nvrhi::BindingLayoutHandle> AdditionalBindingLayouts;`.
  - `Engine/Source/Runtime/Private/Renderer/RayTracing/FRayTracingPipeline.cpp:125-131` implements `AddBindingLayout` (pushes to `AdditionalBindingLayouts`).
  - `FRayTracingPipeline.cpp:158-161` appends `AdditionalBindingLayouts` to `PipelineDesc.globalBindingLayouts` in `FinalizePipeline()`.
  - `FRayTracingPipeline.cpp:402` clears `AdditionalBindingLayouts` in `Shutdown()`.
  - `Engine/Source/Runtime/Private/Renderer/GI/FGIPass.cpp:317` calls `RTPipeline.AddBindingLayout(UAVBindingLayout);` (the v22-split second layout).
  - `Engine/Source/Runtime/Private/Renderer/Shader/GI/GIPathTracing.hlsl:88` and `:91` use `register(u0, space1)` and `register(u1, space1)` for the UAVs.
  - `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/GIPathTracing.hlsl:88` and `:91` mirror those same `space1` registrations (data-dir copy in sync).
  - The fresh-evidence scan's pre-apply integrity gate (`fresh-evidence-scan-v93.sh` §A) would now FAIL with `exit 10` (PATCH-ALREADY-APPLIED) for all three anchor checks: `AdditionalBindingLayouts` exists in the header, `register(u0, space1)` exists in both GIPathTracing.hlsl copies, and `ContainerDefinition.h` is already included. **This means the v93 diagnosis is structurally a no-op re-apply**, not an in-flight fix.
- **Diagnostic implication (file-only):** the `docs/DIAGNOSTIC_2026-07-30.md` hypothesis "v22 split is half-applied: missing second binding-layout push + missing `, space1` on UAVs" is FALSIFIED on disk. Both pieces of v101 are in place. If `HLVM_PT_DEBUG_MODE=20` (raw SRV read of `GBufferMaterial`) still returns zero, the binding bug is **not** what v101 was designed to fix. The remaining suspects (from DIAGNOSTIC_2026-07-30.md §"Root cause" items 1–4) become the live hypothesis set:
  1. slangc dead-stripped the new case 20/21/22 labels (cheap check: `spirv-cross --reflect GIPathTracing.spv` — requires terminal).
  2. Image layout transition on GBuffer* textures is wrong relative to the v22 split (cheap check: re-enable Vulkan validation layer — requires terminal + recompiling nvrhi validation TU, which is the bigger fix the cron was attempting).
  3. nvrhi's `addBindingSet` on a non-immediate command list silently drops the second set (cheap check: enable Vulkan validation layer at submit, look for "second descriptor set not bound" — requires terminal).
  4. Texture handle identity mismatch between raster pass and GI dispatch (cheap check: log `reinterpret_cast<uintptr_t>(Desc.GBufferMaterial.Get())` per frame and compare to the raster-pass `GBufferMaterial` — requires terminal).
- Dumps directory contains 3 fresh groups from 2026-07-30 (00:15:37, 00:15:45, 00:18:15) plus 2 stale groups (2026-07-29 23:55:03/55/56, 2026-07-27 00:07:06-08). Tick 104's claim that "newest dump stamp remains 2026-07-27_000706–08 (4+ days stale)" is **wrong** — a parent runspace *did* produce fresh dumps today at 00:18, and the latest `TestReSTIR_GI_Temporal.log` confirms `gi_raw R[0.000,0.000] G[0.000,0.000] B[0.000,0.000]` (the v93 symptom persists despite v101 being on disk). Newest log: `Engine/Source/Runtime/Binary/Debug/TestReSTIR_GI_Temporal.log` 2026-07-30 00:18:17.
- **What the parent can act on without re-enabling the cron toolset** (Option B from `docs/PIPELINE_OUTER_ESCALATION_2026-07-29.md`):
  1. **Open the 3 fresh dump groups** with vision analysis. The 2026-07-30 00:18 group is the most recent. If `display_frame8.png` is uniform color / dim gray / magenta, the binding bug is still alive even after v101 was applied. If it shows recognizable Sponza, v101 actually fixed it and only the test harness / dump path is stale.
  2. **Run `spirv-cross --reflect Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/GIPathTracing.sblob`** (or the underlying .spv after `Build.sh` extracts it). Confirm `(set=0, binding=1)` for `GBufferMaterial`, `(set=0, binding=2)` for `GBufferNormal`, `(set=0, binding=3)` for `GBufferWorldPos`. If any of those is missing, slangc dead-stripped the binding (suspect 1 above). If they're at `(set=1, ...)` or different binding indices, the v101 patch is incomplete or wrong.
  3. **Diff `Engine/Source/Runtime/Binary/Debug/TestReSTIR_GI_Temporal` mtime against `Engine/Source/Runtime/Private/Renderer/Shader/GI/GIPathTracing.hlsl` and `Engine/Source/Runtime/Private/Renderer/GI/FGIPass.cpp`.** If the binary is older than the source files, the rebuild never picked up the v101 HLSL changes — the modes 20/21/22 dumps in the 2026-07-30 00:18 group would be from the pre-v101 sblob. This is the single most likely explanation: the parent rebuild did not happen, or did not run ShaderMake to recompile the shader.
- 87th+ consecutive stall-loop tick (>80% overlap with ticks 17–104). Authoritative remediation stays in `docs/PIPELINE_OUTER_ESCALATION_2026-07-29.md` Options A (reconfigure inner cron `enabled_toolsets` to include `terminal`), B (parent executes the 4-command `fresh-evidence-scan.sh && Build.sh --Rebuild && HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 run && validate_restir_gi.py` recipe), or C (pause both crons and continue interactive debugging). The watchdog does not re-emit the escalation file because the structural situation is unchanged.
- All 7 final-goal acceptance criteria remain UNVERIFIED: Debug build, fresh `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM>=8` run, fresh-log command-list/VUID exclusions, newest-group-only validator/statistics, visual sane-exposure Sponza inspection, and `HLVM_PT_DEBUG_MODE=20` non-zero GBufferMaterial verification cannot be performed from this file-only runspace.
- No `PIPELINE_NUDGE_*.md` written: the inner pipeline is terminal-blocked, not stalled. A nudge from this runspace cannot produce the fresh evidence the inner pipeline needs; the missing input is parent-side terminal authorization or a toolset reconfiguration, not another marker. The 3-step recipe in the paragraph above gives the parent a path to advance without re-engaging the cron.
- No source/test edit, role marker cycle, completion marker, Kanban action, commit, push, history rewrite, or fabricated PASS was performed. Hard rules #1–#10 all honored. PENDING_PICK v126/v127/v128/v129 parent-evidence gate enforced; no six-role cycle dispatched. This audit append was written via `write_file` (file-only) per hard rule #6 (never silently exit) and hard rule #7 (overseer writes to docs/ only, never pushes).