# Pending Plan v117
- task: restir-gi-fix-runtime-verification-v117 — obtain fresh executable and visual evidence for the v114 split-layout repair
- source: current working-tree v114 implementation recorded by `docs/PENDING_COMMIT_v114.md`; no bundle, patch application, or speculative renderer edit
- approach: Keep renderer and shader source unchanged until execution identifies a concrete failure. In a terminal-authorized role, capture the existing dump/log frontier, build `TestReSTIR_GI_Temporal`, and run it with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; assess only log bytes and a coherent frame-8 dump group newer than the frontier. Completion requires build and process success, no fresh command-list-already-open or Vulkan ERROR/VUID evidence, validator PASS against only that newest group, healthy structural image statistics, and direct inspection confirming recognizable non-uniform Sponza architecture at sane exposure. If execution remains denied, downstream roles must record the exact tool result, claim no acceptance, and requeue without source edits.
- diff_estimate: +0 / -0 production lines on the verification-first path; any later minimal root-cause fix requires fresh failing evidence and a documented plan deviation
- skip_plan_review: no
- test_strategy: Role #5 records dump filenames/mtimes and the current test-log byte size before execution; runs `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal`, then a fresh target test with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8` using the project's non-parallel `Build.sh --Test` path; checks process status and only the appended log region for command-list-already-open diagnostics, Vulkan `ERROR`, and `VUID-`; copies or explicitly enumerates only the newest coherent frame-8 PNG group when invoking `validate_restir_gi.py`; computes display RGB mean/std, black and saturated fractions, 4x4 cell-mean variance, and alpha; and directly inspects the fresh display for Sponza arches, columns, depth, non-uniform lighting, and sane exposure. Retain static controls for `AdditionalBindingLayouts` append/clear, GI UAV slots 384/385, and both `GIPathTracing.hlsl` copies at `register(u0/u1, space1)`.
- risks: This planner tick's terminal probe returned `status=pending_approval`, `exit_code=-1`, `pattern_key=tirith:unknown` before any command output, so all runtime gates remain unverified. Historical dumps and logs predate v114 and cannot be reused; `validate_restir_gi.py` globs historical frame-8 groups unless isolated. The old `fresh-evidence-scan-v93.sh` and `git-apply-preflight-v111.sh` predate the applied v114 repair and must not be run. Scalar statistics can pass on noise or dim gray, so visual inspection is mandatory. Do not run parallel ctest and do not edit GPU source based on stale evidence.

## Failure routing
1. Build failure: preserve the first compiler/linker diagnostic, search the relevant source class for cascade siblings, and route only a minimal evidence-backed fix.
2. Process or fresh-log failure: preserve exit status and exact fresh excerpt; diagnose command-list lifetime or descriptor/layout state before changing shader math.
3. Bad structural or visual output: bisect one variable at a time with existing albedo/normal/direct/GI/hardware-ID/barycentric/sentinel modes and inspect each fresh image.
4. All gates pass: downstream roles may close v117 and write goal-completion evidence; otherwise completion remains prohibited.
