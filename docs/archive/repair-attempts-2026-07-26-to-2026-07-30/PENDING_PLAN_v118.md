# Pending Plan v118
- task: restir-gi-fix-runtime-verification-v118 — obtain fresh executable, log, validator, structural, and visual evidence for the v114 split-layout repair
- source: current working-tree v114 implementation recorded by `docs/PENDING_COMMIT_v114.md`; no bundle, stale helper, patch application, or speculative renderer edit
- approach: Preserve renderer, shader, and test source until a fresh execution identifies a concrete failure. A terminal-authorized role first records the current test-log byte count and existing dump filenames/mtimes, builds `TestReSTIR_GI_Temporal`, and runs the canonical non-parallel test path with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`. It then evaluates only appended log bytes and only a coherent post-frontier frame-8 dump group. Completion requires build/process success, no fresh command-list-already-open or Vulkan ERROR/VUID diagnostics, validator PASS on an isolated directory containing only the newest group, healthy display statistics, and direct image inspection confirming recognizable non-uniform Sponza arches/columns/depth at sane exposure. If terminal remains denied, record the exact tool result, claim no runtime gate, and requeue unchanged without renderer edits.
- diff_estimate: +0 / -0 production lines on the verification-first path; any later minimal fix must be tied to a specific fresh failure and documented as a plan deviation
- skip_plan_review: no
- test_strategy: Role #5 captures the artifact frontier; runs `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal`, then `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 ./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal --Test` without parallel ctest; records both exit statuses; scans only log bytes appended after the frontier for case-insensitive command-list-already-open, Vulkan ERROR, and `VUID-`; selects only post-frontier frame-8 PNGs from one coherent timestamp group, copies/enumerates them in an isolated validation directory, and runs `validate_restir_gi.py` there; computes display RGB channel means/std, black and saturated fractions, 4x4 cell-mean variance, and alpha-sentinel fractions; then directly inspects the display for Sponza architecture, spatially coherent lighting, depth, and sane exposure. Retain static controls for `AdditionalBindingLayouts` append/clear, FGIPass UAV slots 384/385, and both `GIPathTracing.hlsl` copies at `register(u0/u1, space1)`.
- risks: This planner tick's terminal probe again returned `status=pending_approval`, `exit_code=-1`, `pattern_key=tirith:unknown` before command output, so all runtime gates remain unverified. The only existing frame-8 group is the stale 2026-07-27 seven-PNG group and cannot be reused. `validate_restir_gi.py` globs every `*frame8.png` in its argument directory, so running it against the historical dump directory violates newest-group-only acceptance. The pre-v114 `fresh-evidence-scan-v93.sh` and `git-apply-preflight-v111.sh` must not run because they can reject or mutate the already-applied repair. Scalar statistics can pass noise or dim gray; direct inspection is mandatory.

## Failure routing
1. Build failure: preserve the first compiler/linker diagnostic, search the whole relevant source class for cascade siblings, and route one minimal evidence-backed fix.
2. Process or fresh-log failure: preserve exit status and exact appended-log excerpt; diagnose command-list lifetime or descriptor/layout state before shader math.
3. Validator, structural, or visual failure: bisect one variable per run using existing albedo/normal/direct/GI/hardware-ID/barycentric/sentinel modes and inspect each fresh image.
4. All gates pass: downstream roles may write goal-completion evidence; otherwise completion remains prohibited.
