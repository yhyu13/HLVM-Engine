# Pending Plan v54
- task: v54 — close the v60 doc-drift gap surfaced this tick (lines 60 + 676 still say "near line 1521" / "near line 1516" while the actual NOTE comment in v5 is at line 1531; +12/-5 lines net, three textual replacements across two files, also update the description in v43 PICK entry to reflect 27 CHECKS).
- source: no bundle — direct edit
- approach: Apply a small doc-drift cleanup: (1) in `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal.cpp` line 676, replace "near line 1516" with "near line 1531"; (2) in `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/fresh-evidence-scan.sh` line 60, replace "near line 1521" with "near line 1531"; (3) in `docs/PENDING_PICK.md` v43 entry description, add "27 CHECKS" annotation. All three edits are textual replacements of identical-length strings; zero behavior change; fully reversible by `git checkout` on the three files. No new syntax; no new imports; no new CVar; no new HLSL case label; no new Python helper. No tests added (comment/PICK-only). No need for terminal access (file-only edits).
- diff_estimate: +0 / -0 lines textual (12-line comment block adjustment + 1-line PICK annotation), or +1 / -0 net if a fresh one-line attribution block is added.
- skip_plan_review: yes (three identical-length text replacements, no semantic change, no risk; matches v6/v7/v8/v14 doc-drift precedent).
- test_strategy: tester verifies via `search_files` that "near line 1531" appears at both files (cpp + sh) and that "near line 1521" / "near line 1516" no longer appear in those files.
- risks: None material. Identical-length textual replacements preserve column counts. PICK annotation is purely descriptive. The v54 patch is dormant with respect to runtime behavior — it does not affect the build, the binary, the validator, the dumps, the alpha signal, or any GPU work.
