# Outer watchdog heartbeat — 2026-07-27 v24

**Final-goal gate FAILED; new auxiliary-check defect found:** Latest complete six-role artifacts remain v20 (`PENDING_PLAN_REVIEW_v20=KEEP`, `PENDING_IMPL_REVIEW_v20=KEEP`, `PENDING_TEST_AUDIT_v20=SOME_RELAX`), while v21 is still gated on absent `rgi_evidence.txt`. Static inspection found that v20's `run_rgi_diagnostic.sh` mis-rotates dumps: each loop archives the previous run under the current mode, so stale pre-run `dumps/` becomes `dumps_default`, fresh default output becomes `dumps_mode6`, later modes remain shifted, fresh mode99 is deleted, and lines 106–111 restore stale `dumps_default` for validation. The runner therefore fails per-mode isolation, newest-group validation, and auxiliary-check requirements and must be fixed by the inner pipeline before its evidence is trusted. Independently, newest actual dumps remain `20260727_000706`–`000708`; their matching log completes eight frames but contains seven `A command list should be executed before it is reopened` warnings and zero `gi_raw` RGB ranges, so the render gate fails. Tirith blocked shell/git/build/run/validator/image-stat access, and visual Sponza verification remains pending. No `PIPELINE_GOAL_DONE` or `PIPELINE_NUDGE` was written because explicit failures explain the wait (no unexplained stall or FIX→FIX loop); no source/governance edit, commit, push, merge, pause, block, archive, or card creation was performed.
