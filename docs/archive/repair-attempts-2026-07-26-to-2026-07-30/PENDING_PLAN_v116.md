# Pending Plan v116
- task: restir-gi-fix-runtime-verification-v116 — retry the unchanged verification-first GPU acceptance contract in a terminal-authorized runspace
- source: current working-tree v114 implementation recorded by `docs/PENDING_COMMIT_v114.md`; no bundle, patch application, or speculative renderer edit
- approach: Preserve the v114 split-layout source unchanged until a fresh build/run supplies a concrete failure. Build `TestReSTIR_GI_Temporal`, record the pre-run dump/log frontier, then run with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; evaluate only log bytes and the coherent frame-8 dump group newer than that frontier. Require a successful process, no command-list-already-open or Vulkan ERROR/VUID evidence, validator PASS on only the newest group, structural image variation, and direct visual confirmation of recognizable non-uniform Sponza geometry at sane exposure. If execution is denied again, record the exact blocker downstream and requeue without claiming acceptance or editing GPU source.
- diff_estimate: +0 / -0 production lines on the verification-first path; a later root-cause fix is permitted only after fresh failing evidence and must be documented as a plan deviation
- skip_plan_review: no
- test_strategy: Role #5 runs `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal` (and the target's non-parallel `--Test` form if supported), snapshots dump/log names and mtimes before execution, launches the freshly built target with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`, and checks only newly written evidence. Scan the fresh log region case-sensitively for command-list-already-open diagnostics plus Vulkan `ERROR`/`VUID-`; isolate the newest coherent `*_frame8.png` group for `validate_restir_gi.py` rather than allowing historical glob mixing; compute RGB mean/std, black/saturated fractions, 4x4 cell-mean variance, and alpha; inspect the new display PNG for recognizable Sponza arches/columns/plaza and sane exposure. Static controls must also retain both `space1` UAV declarations, slots 384/385, the added global layout, and shutdown cleanup.
- risks: This tick's terminal probe was again denied as `pending_approval: tirith:unknown`, so no new executable evidence exists and all six runtime acceptance gates remain unverified. The bundled `fresh-evidence-scan-v93.sh` and `git-apply-preflight-v111.sh` predate v114 and reject/alters already-applied source; they are stale and must not be run. `validate_restir_gi.py` scans historical groups unless inputs are isolated. Numeric image statistics are necessary but not sufficient: no completion verdict is allowed without looking at the fresh display image.

## Failure routing
1. Compiler/link failure: preserve the first diagnostic, search the full relevant source class for cascade siblings, and route a minimal fix to the impler.
2. Process/log failure: preserve exit code and fresh log excerpt; diagnose command-list or descriptor/layout state before shader edits.
3. Structurally or visually bad output: bisect one variable at a time using existing GBuffer/direct/GI/hardware-ID/barycentric/sentinel modes.
4. Every fresh gate passes: downstream roles may close v116 and write goal-done evidence; otherwise no goal marker.
