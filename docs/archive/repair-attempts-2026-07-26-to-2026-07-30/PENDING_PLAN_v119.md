# Pending Plan v119
- task: restir-gi-fix-runtime-verification-v119 — obtain fresh executable, log, validator, structural, and visual evidence for the v114 split-layout repair
- source: current working-tree v114 implementation recorded by `docs/PENDING_COMMIT_v114.md`; no bundle, stale helper, patch application, or speculative renderer edit
- approach: Preserve renderer, shader, and test source until fresh execution identifies a concrete failure. A terminal-authorized impler records the current test-log byte count and existing dump filenames/mtimes, builds `TestReSTIR_GI_Temporal`, and runs the canonical non-parallel path with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`. It must evaluate only appended log bytes and one coherent post-frontier frame-8 dump group; completion requires build/process success, no fresh command-list-already-open or Vulkan ERROR/VUID diagnostics, newest-group-only validator PASS, healthy structural statistics, and direct image inspection confirming recognizable non-uniform Sponza arches/columns/depth at sane exposure. If execution remains externally blocked, record the exact tool result, claim no runtime gate, and requeue unchanged without renderer edits.
- diff_estimate: +0 / -0 production lines on the verification-first path; any later minimal fix must be tied to a specific fresh failure and documented as a plan deviation
- skip_plan_review: no
- test_strategy: Role #5 establishes an artifact/log frontier; runs `./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal`, then `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 ./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal --Test` without parallel ctest; records both exit statuses; scans only appended log bytes for case-insensitive command-list-already-open, Vulkan ERROR, and `VUID-`; selects only post-frontier frame-8 PNGs from one coherent timestamp group and validates an isolated directory containing only that group with `validate_restir_gi.py`; computes display RGB means/std, black and saturated fractions, 4x4 cell-mean variance, and alpha-sentinel fractions; and directly inspects the display for recognizable Sponza architecture, coherent lighting/depth, and sane exposure. Retain static controls for `AdditionalBindingLayouts` append/clear, FGIPass UAV slots 384/385, and both `GIPathTracing.hlsl` copies at `register(u0/u1, space1)`.
- risks: This planner tick's terminal lock/probe command was again denied before output as `status=pending_approval`, `exit_code=-1`, `pattern_key=tirith:unknown`; this is not build evidence, so every runtime/visual gate remains unverified. The existing 2026-07-27 frame-8 group and pre-v114 log are stale and prohibited. `validate_restir_gi.py` globs every historical `*frame8.png` in its argument directory, so the newest group must be isolated. The pre-v114 `fresh-evidence-scan-v93.sh` and `git-apply-preflight-v111.sh` may reject or mutate the already-applied repair and must not run. Scalar validator/statistics can pass noise or dim gray, so direct image inspection is mandatory.

## Failure routing
1. Build failure: preserve the first compiler/linker diagnostic, search the whole relevant source class for cascade siblings, and route one minimal evidence-backed fix.
2. Process or fresh-log failure: preserve exit status and exact appended-log excerpt; diagnose command-list lifetime or descriptor/layout state before shader math.
3. Validator, structural, or visual failure: bisect one variable per run using existing albedo/normal/direct/GI/hardware-ID/barycentric/sentinel modes and inspect each fresh image.
4. All gates pass: downstream roles may write goal-completion evidence; otherwise completion remains prohibited.
