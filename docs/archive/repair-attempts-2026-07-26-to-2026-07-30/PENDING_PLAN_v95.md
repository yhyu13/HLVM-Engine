# Pending Plan v95

- task: restir-gi-fix — **DIAGNOSIS-DEEPENED** (semantic continuing v93 ROOT_CAUSE_NAMED, v94 RUNSPACE_BLOCKED, v95 file-only super-cession)
- source: no bundle — file-only diagnostic deepening from PENDING_PLAN_v93/v94 plus fresh cross-tick re-verification this turn
- approach: 5 NEW Part A probes pin down two previously-unverified facts about the GI pipeline binding wiring: **(i) the dumper at TestReSTIR_GI_Temporal.cpp:1734 unconditionally forces Pixels.a = 1.0f per pixel, so the v28 alpha-sentinel at GIPathTracing.hlsl:694 is INVISIBLE in every dumped PNG** (any image's alpha channel is uniformly 255); **(ii) the v22 split's "register UAVBindingLayout as second pipeline binding layout" requires an API surface that DOES NOT exist** — FRayTracingPipeline.h exposes only `BindingLayout` (single) + optional `BindlessLayout` push via `globalBindingLayouts.push_back()`. There is NO `AddBindingLayout(ExternalLayout)` method. The working sibling TestCornellBoxGI.cpp:831-842 uses a SINGLE binding layout with `AddTextureUAV(0)` next to the SRVs — that pattern is the canonical fix. **Conclusion: the v93 fix recipe's branch (a) "push UAVBindingLayout into PipelineDesc.globalBindingLayouts" requires adding a new API method to FRayTracingPipeline; branch (b) "collapse back to single binding set like CornellBox sibling" is the minimal fix (~5 hunks across FGIPass.cpp + GIPathTracing.hlsl BOTH copies, no API surface change) and is the recommended path.**
- diff_estimate: +5 lines / -7 lines (collapse-back option) OR +25 lines / -10 lines (add-API option)
- skip_plan_review: no — diagnosis confirms v93 hypothesis direction but REDUCES the fix-surface from "3 files" to either "5 files (API change)" or "3 files (collapse)". Plan-criticer must validate the choice.
- test_strategy: tester (role #5) writes a small inline shell recipe: `grep "register(u0)\|register(u1)" Engine/Source/Runtime/.../GIPathTracing.hlsl`, `grep "globalBindingLayouts\|UAVBindingLayout\|BindingLayout = " FRayTracingPipeline.h/cpp`, `grep "Pixels\[DstIdx + 3\] = 1" TestReSTIR_GI_Temporal.cpp`. Parent on terminal action prefers patch recipe (collapse-back) + rebuild + re-run + validator + open dump and look at it.
- risks: (a) collapse-back reintroduces the nvrhi-deferred-barrier-ordering pattern that the v22 split was created to fix. The sibling TestCornellBoxGI works because Cornell box has no `gbuffer_worldpos` SRV binding doing SHADER_READ_ONLY transitions on a different resource family. FGIPass has many SRVs (gbuffer t1/t2/t3, RTVertices t5, RTIndices t6, RTInstanceInfo t7/t8, Lights t7, sampler s2). Adding one Texture_UAV to that mix DOES reintroduce the VUID-00344 warning. The fix needs careful re-application of the v22 split without losing the second-binding-set dispatch capability — meaning **the right fix is actually (a) add `void FRayTracingPipeline::AddBindingLayout(nvrhi::BindingLayoutHandle)` API + call it from FGIPass right before FinalizePipeline**. The v22 split's architecture is correct; only the pipeline-registration step is missing. (b) The v28 alpha-sentinel mask (finding (i)) means a passing validator cannot distinguish "dispatch wrote only alpha" from "dispatch wrote RGB" unless we either add a new dumper that preserves alpha or rely solely on the v3 ENTER/EXIT log. The repair recipe needs to add either an alpha-preserving dump or a stderr-only post-raster readback.
