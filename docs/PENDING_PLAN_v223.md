# Pending Plan v223

- task: Fix wrong comment at `Engine/Source/Runtime/Shader/BilateralDenoise_cs.hlsl:26-32` that misrepresents the selection mechanism for the SHARED/default copy. Card S from PENDING_PICK.md.
- source: no bundle — direct edit
- approach: Replace the comment block lines 26-32 with one that names the real selection mechanism. Each consumer selects which copy to compile by passing its own `DataDir` argument to `FBilateralDenoisePass::Initialize` (see `FBilateralDenoisePass.cpp:44,48` — the constructor stores `InShaderDataDir` and combines it into `BilateralDenoise_cs.sblob`). The `FCommonRenderPasses::SetShaderDataDir()` override applies only to Blit resources (see `FCommonRenderPasses.cpp:290-298` which resets Blit init; the only consumer of `g_ShaderDataDirOverride` is `GetShaderDataDir():67-71`, whose sole consumer is `InitBlitResources(:319,:322)` which loads only `BlitVS.sblob`/`BlitPS.sblob`). The shared comment should also retain the warning that v211 captured: a future editor must keep guide handling aligned across all three copies.
- diff_estimate: +5 / -6 lines (one file)
- skip_plan_review: no — even though it is comment-only, the plan-criticer should re-derive the override's scope and the Initialize `DataDir` argument chain (card S v215 finding: a marker's description of source is evidence about the marker, not about the code).
- test_strategy: role #5 (tester) re-derives that `SetShaderDataDir` has 0 callers in the whole Engine tree and that `FBilateralDenoisePass::Initialize`'s `DataDir` argument is the load path. Role #6 (testing-verifier) checks the new comment does not anchor `old_string` on the statement boundary above a cbuffer initialiser (v203 near-miss geometry).
- risks:
  - **v203 near-miss geometry.** Anchoring `old_string` on the comment block before the `cbuffer Constants` initialiser (line 15) could match the cbuffer braces and corrupt the cbuffer. Anchor must be on a unique statement boundary **inside** the misleading block (the literal text "FCommonRenderPasses uses it unless" or similar) or replace the block as a contiguous range — not on the line preceding the cbuffer.
  - **Stale-line citation.** v215/v217 established that counts survive across cycles but line numbers do not. The plan cites `FBilateralDenoisePass.cpp:44,48` and `FCommonRenderPasses.cpp:290-298,319,322`; if a future merge shifts them, the comment's correctness does not depend on the line numbers because the new comment names the *symbol* (`FBilateralDenoisePass::Initialize`, `FCommonRenderPasses::SetShaderDataDir`, `InitBlitResources`) and the *behaviour*, not the *line number*.
  - **Symmetric risk.** v211 found a third live-but-stale copy at this same path; this cycle does not touch the other two copies (`Engine/Source/Runtime/Test/TestCornellBoxGI_Data/BilateralDenoise_cs.hlsl`, `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/BilateralDenoise_cs.hlsl`). Both are governed by their own `ShaderMake.cfg`; their selection mechanism is also `DataDir`, not the override. Deliberately NOT bundled — see card S's "Deliberately not patched in this cycle" footer; it would require patching both for the comment to be uniformly true across the copies.
  - **Anti-condition check.** The 3 anti-conditions from `six-role-pipeline §When NOT to use this skill` are: (i) interactive GPU debug — NO; this is a comment-only edit on a file the build does not load any differently (the comment is documentation, not control flow). (ii) single-line surgical patch — the edit is +5/-6 lines, multi-line comment block, not a 1-2 line typo. (iii) single-profile file-only host with terminal blocked — irrelevant to a comment edit. None of the anti-conditions apply. The skill IS appropriate here.
