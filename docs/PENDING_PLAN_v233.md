# Pending Plan v233 — Verification card (no code change) for the v232 closure

- task: codify the operator-side closure path that satisfies all 7 user-stated acceptance gates. v232 cycle (W reservoir clamp) is closed on disk; the operator-side run + validator + vision chain is the only remaining gap, and that chain already exists at `_OPERATOR_RECIPE_v176.sh` + `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` + `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/validate_restir_gi.py`. v233's job is to surface this pre-existing infrastructure explicitly, document the discriminator protocol for each gate (especially gate 7 — `HLVM_PT_DEBUG_MODE=20` non-zero GBufferMaterial — which the lineage has empirically passed in past runs but which the v8 audit only marked "PASS by contrapositive"), and freeze the closure recipe as the authoritative next-step.
- source: no bundle — direct edit of `docs/` markers only. No C++ / HLSL / shader changes. No `git add` / `git commit` / `git push` (per HARD INVARIANT and per "do not commit, push, or modify governance files" in user instruction).
- approach:
  1. **Surface the existing infrastructure.** Three files already implement the closure path:
     - `_OPERATOR_RECIPE_v176.sh` (53-line repo-root shim, dated 2026-08-19, tick-300) — invokes the canonical recipe.
     - `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` (489 lines) — implements all 7 gates with exit codes 0-7 (BUILD=1, DUMP=2, VULK=3, CMDL=4, VAL=5, M20=6, ENV=7) and supports `--mode-20`, `--mode-30`, `--mode-31` discriminators.
     - `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/validate_restir_gi.py` (404 lines) — the 4-check structural validator (`check_black_ratio`, `check_color_variance`, `check_temporal_stability`, `check_cell_variance`).
  2. **Surface the lineage evidence chain.** `search_files path=docs pattern="PT_DEBUG_MODE=20"` returns 30+ hits across the lineage:
     - `PIPELINE_HEALTH_2026-08-15_six-role-tick1622.md:49`: "PASS (std=[0.162,0.156,0.129] non-uniform verified this tick)" — mode-20 returned non-zero.
     - `PIPELINE_HEALTH_2026-08-22_six-role-tick-now-696.md:92`: mode-20 discriminator recipe referenced.
     - `PIPELINE_HEALTH_2026-08-24_six-role-tick-now-480.md:117`: "PASS file-only (line 246 `gbuffer_material std=[0.1622, 0.1563, 0.1291]` ≥ 0.16)" — mode-20 non-zero in fresh log.
     - `PIPELINE_HEALTH_2026-08-06_tick918.md:85`: "PASS INDIRECT — gi_raw std=[0.78, 0.78, 0.79] confirms non-zero per-channel variance from GBufferMaterial SRV".
     - `PIPELINE_HEALTH_2026-08-06_tick940.md:57`: "PASS INDIRECT" — same chain.
     This evidence refutes `DIAGNOSTIC_2026-07-30.md`'s "mode 20 returns zero" claim at the artifact level (subsequent log runs show non-zero). The diagnostic was a snapshot from the v22-split era (before v214 reverted the split); post-v214 logs confirm the binding chain works.
  3. **v232 patch is on disk but UNBUILT** (per v8 audit's section (c) — freshest log line 262 still shows pre-patch `G max=59044` confirming pre-clamp W). The rebuild is operator-side (`./Build.sh --Config=Debug --Target=TestReSTIR_GI_Temporal --Rebuild`). The closure recipe runs this rebuild as gate 1.
  4. **No new code patches.** The v233 cycle ships no new files. The deliverable is documentation: PENDING_* markers that document the closure path, plus this turn's PIPELINE_HEALTH audit. Per `six-role-pipeline §Anti-patterns §5` ("For true single-line fixes, bypass the pipeline entirely and do a direct edit with a one-shot verification step"), the pipeline overhead is justified here ONLY because the user explicitly invoked it AND the verification scaffolding already exists on disk — this cycle is a 1-step file-only seam that surfaces what is already there.
- diff_estimate: +0 / -0 source code lines. +6 documentation files (`PENDING_PLAN_v233.md`, `PENDING_COMMIT_v233.md`, `PENDING_PLAN_REVIEW_v233.md`, `PENDING_IMPL_REVIEW_v233.md`, `PENDING_TESTS_v233.md`, `PENDING_TEST_AUDIT_v233.md`) + 1 PIPELINE_HEALTH audit (this turn's). All under 50-line mark, qualifies as trivial per anti-pattern §5.
- skip_plan_review: no — the lineage evidence chain deserves explicit second-head audit (plan-criticer's job is to weigh whether "non-zero mode-20 in past logs" actually refutes "DIAGNOSTIC_2026-07-30.md claim" given that the diagnostic is from 2026-07-30 and the lineage PASS lines are from 2026-08-15 onward, AFTER v214 reverted v22's split). This is the load-bearing judgment of the cycle.
- test_strategy: tester role #5 runs file-only verifier confirming (a) `_OPERATOR_RECIPE_v176.sh` exists and is executable-marked, (b) `v176-recipe.sh` is 489 lines and handles gates 1-7 with explicit exit codes, (c) `validate_restir_gi.py` is 404 lines and implements the 4-check structural validator, (d) at least 3 lineage PIPELINE_HEALTH entries cite mode-20 PASS with explicit log-line evidence. testing-verifier role #6 audits for the 5 broken-test patterns. Operator-side acceptance is `_OPERATOR_RECIPE_v176.sh` running with exit 0 from a shell.
- risks:
  1. **Past mode-20 PASS evidence may be stale** if a subsequent cycle broke the binding chain. Mitigation: the v232 patch (W clamp) is in `ReSTIR_Temporal_cs.hlsl` and `ReSTIR_Spatial_cs.hlsl` — neither touches `GIPathTracing.hlsl`'s t1/t2/t3 SRV reads. So the binding chain that worked post-v214 cannot have been broken by v232.
  2. **The freshest log line 262 still shows pre-patch `G max=59044`** (per v8 audit). This is because the v232 patch is on disk but the binary hasn't been rebuilt. After rebuild, gate 6 (validator) will see fresh dump; gate 7 (mode-20) will see fresh dump. Both will reflect post-v232 + post-v214 state.
  3. **The v176-recipe.sh and v232 PICK were authored by the same single-profile lineage.** Per `six-role-pipeline §Anti-patterns §7`, single-profile caveat applies. The reviewer role's KEEP verdict is a self-audit, not independent. Mitigation: the lineage evidence chain (multiple past PASS lines for mode-20) is the second signal; the operator-side run is the third.
  4. **This is a documentation-only cycle, not a code change.** Per `six-role-pipeline §Anti-patterns §5`, this should "bypass the pipeline entirely and do a direct edit with a one-shot verification step." I am running it through the pipeline because (a) the user explicitly invoked the skill and asked for cycles, (b) the audit-trail is itself a deliverable the user wants (per "Keep a concise append-only docs/PIPELINE_HEALTH_YYYY-MM-DD.md audit"), and (c) the verifier-rows in the tests marker are non-trivial and benefit from review.
- relation to existing queued cards: this cycle does NOT touch the closed `[x]` card on PICK line 5. It produces documentation that codifies the operator-side closure path. If the operator runs the recipe and any gate fails, the failure signature (`exit 5` = validator, `exit 6` = mode-20, etc.) maps to a new card that becomes v234+.
