# Pending Plan v157
- task: TestReSTIR_GI_Temporal acceptance verification — file-only cycle-stop re-affirmation
- source: no bundle — verification-only cycle against the existing v137, v140, and v151 fixes
- approach: Same as v150/v155/v156: a fresh Debug build, a fresh `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8` non-bypass run, a fresh `HLVM_PT_DEBUG_MODE=20` discriminator run, the `validate_restir_gi.py` validator on the newest dump group, numpy per-pixel statistics, and vision-check of the newest `*_display_frame8.png`. No production edit is authorized unless fresh evidence identifies a concrete failing boundary; any resulting fix must start a new RED/bisect cycle rather than being guessed from the stale 2026-07-30 diagnostic.
- diff_estimate: +0 / -0 production lines for verification; any evidence-driven fix requires a subsequent reviewed plan
- skip_plan_review: no
- test_strategy: Build `TestReSTIR_GI_Temporal` in Debug; run the non-bypass target with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; run a mode-20 discriminator with the same settings; execute `validate_restir_gi.py` against the newest dump group only; use numpy per-pixel/channel statistics and visual review of the newest `*_display_frame8.png`; require mode 20 to be non-zero and spatially varying and require fresh logs to contain no Vulkan VUID/ERROR or command-list errors.
- risks: This scheduled runspace's terminal request is blocked again this tick with `status: pending_approval` / `pattern_key: tirith:unknown` for `date; pwd` and earlier probes. No vision tool is registered. Existing 2026-08-08 logs and PNGs are useful historical evidence but cannot be labeled fresh for this cycle. The authoritative 2026-07-30 mode-20 zero diagnosis predates the v137 zero-binding-offset fix and must not be treated as current runtime evidence. Do not modify production code, mark the PICK card complete, or claim validator/vision success without fresh execution evidence.