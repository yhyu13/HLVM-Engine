# Pending Plan v229

- task: **Card L** (PENDING_PICK line 84) — tenth instance of the Phase-D/extent class, in the known-good control `TestCornellBoxGI.cpp`. 14 ReSTIR/denoise textures created once at init from startup framebuffer extent; never recreated; dispatched over them at `CurrentFBInfo` every frame with `Resizable = true`. Both compute guards are tautological, so a widened window = unguarded out-of-bounds UAV store.
- source: no bundle — direct edit of `Engine/Source/Runtime/Test/TestCornellBoxGI.cpp` (lines 951-1021 creation block, line 1160-1256 resize branch)
- approach: **Extract a free static helper** `CreateReSTIRTextures(nvrhi::IDevice* Device, uint32_t W, uint32_t H, FReSTIRPassTextures& Out)` in `Engine/Source/Runtime/Test/TestCornellBoxGI.cpp` (file-local, no header change). Helper creates the 14 textures + the 3 `clearTextureFloat` history clears. Helper is called from init (one-shot, current behavior preserved) and from the resize branch (mirroring `TestRTReflections.cpp:892-984` positive control). **No explicit release** — nvrhi::TextureHandle is reference-counted; reassigning the member drops the old handle (verified by reading the existing resize branch which also reassigns GBuffer MRTs without explicit release).
- diff_estimate: +34/-30 lines net; one new file-local helper (~30 lines), the 14 createTexture calls + 3 clears move into the helper, init block becomes a single call site (~10 lines), resize branch gains a call site (~5 lines). Single file (`TestCornellBoxGI.cpp`). No header change.
- skip_plan_review: no (already reviewed; this is the FIX revision)
- test_strategy: role #5 (tester) writes file-only checks that pin the cycle's structural invariants:
  - **(i)** `search_files pattern="NvrhiDevice->createTexture" path=TestCornellBoxGI.cpp` → count BEFORE patch = 24 (8 GBuffer+staging from resize + 14 from init + 2 from elsewhere, exact count to be re-verified by impler); count AFTER patch = 24 (unchanged at call sites — the same `createTexture` calls exist, just relocated). **The DELTA the test should pin**: `search_files pattern="CreateReSTIRTextures" path=TestCornellBoxGI.cpp` → exactly 2 hits (one definition, one call from init — the resize call appears in a different lexical position but still inside the resize branch).
  - **(ii)** `search_files pattern="clearTextureFloat" path=TestCornellBoxGI.cpp` → exactly 3 hits before and after patch (the 3 history clears); verify each target is `Reservoir0HistoryTexture`, `Reservoir1HistoryTexture`, or `RadianceHistoryTexture` (not GBuffer textures).
  - **(iii)** `search_files pattern="Reservoir0Texture = " path=TestCornellBoxGI.cpp` → exactly 1 hit, inside the helper. Same for `Reservoir1Texture`, `Reservoir0HistoryTexture`, `Reservoir1HistoryTexture`, `Reservoir0MergedTexture`, `Reservoir1MergedTexture`, `ReSTIROutputTexture`, `TemporalRadianceTexture`, `RadianceHistoryTexture`, `PrevDepthTexture`, `PrevNormalTexture` (11 of the 14; the helper will create the 3 history clears via `clearTextureFloat`, not createTexture).
  - **(iv)** `search_files pattern="bReSTIRInitialized" path=TestCornellBoxGI.cpp` → count unchanged from current (3 hits: declare, init-set, render-guard).
  - **(v)** `search_files pattern="BindingCache.Clear" path=TestCornellBoxGI.cpp` → exactly 1 hit, inside the resize branch at `:1255` (unchanged).
  - **(vi)** `search_files pattern="RenderGBuffer|CmdList->open|CmdList->close" path=TestCornellBoxGI.cpp` → counts unchanged (the helper does NOT execute a command list itself; it relies on the caller to issue the clear cmdlist).

- risks:
  - **`TestCornellBoxGI.cpp` is the known-good control** — any defect introduced here surfaces as a failure in tests that exonerate the driver/framework for unrelated cycles. The cycle's read-back must verify the resize branch is **functionally equivalent** to the init block (every texture created with same desc, every clear preserved, every member assigned).
  - **`BindingCache.Clear()` at `:1255`** is in the resize branch and must remain so — ReSTIR binding sets reference the 14 textures and need clearing after recreate.
  - **`ReSTIRPass.Initialize`** at `:1023` is BEFORE the textures go into the binding sets (it's an FReSTIRPass internal resource init, not a per-frame binding). Moving texture creation does NOT change when Initialize is called. **The helper is called BEFORE `ReSTIRPass.Initialize`** (so that the textures exist when Initialize needs them) — same ordering as today.
  - **First-frame hazard**: if the resize branch runs on the very first frame (i.e. `GBufferNormalsTexture == nullptr`), the textures are still null, so the helper must work both as the *first call* (init) and as a *re-call* (resize). It does, because it always overwrites the members.
  - **`bReSTIRInitialized` ordering**: init sets it to `true` at `:1028` after `ReSTIRPass.Initialize`. If the helper runs as part of resize, `bReSTIRInitialized` is already true (init succeeded on first run), so no ordering issue. The render-guard at `:1501` `if (bReSTIRInitialized)` correctly gates dispatch.
  - **Sibling pattern**: `TestRTReflections.cpp:892-984` is the positive control v198's set-difference procedure established (tick-545 closure): it recreates all extent-sized resources on resize. The fix follows that exact shape.
  - **v182 dual-copy hazard**: this card touches `TestCornellBoxGI.cpp` only. There is no `TestCornellBoxGI_Data/Cornell_HLSL_*` mirror; the ReSTIR_*_cs.hlsl files in `TestCornellBoxGI_Data/` are read by ShaderMake but not edited here.
  - **tick-526 alternation rule**: no `|` in any pattern; one term per query.
  - **tick-538 regex metachar rule**: no regex special chars; substring matches only.
  - **tick-538 dot-directory rule**: `path=` is always a FILE or non-hidden directory.
  - **Precondition DISSOLVED**: card L's "do NOT action while v183-v198 chain is unbuilt" gate is now satisfied — `Engine/Source/Runtime/Binary/Debug/TestReSTIR_GI_Temporal` binary exists on disk (built 2026-08-22 01:02:23 → 01:02:42), 282-line log shows clean 19.33s completion, 0 VUID/ERROR, display stats `mean=[0.5688,0.5665,0.5822] std=[0.0877,0.0899,0.0891]` = recognizable Sponza at sane exposure, `gbuffer_material std` non-zero, `ReSTIR summary M mean=7.71 max=16.0 (MaxM=30)` = 2.6× the 2.93 from 2026-08-14 — chain built successfully and the control's exonerating role is preserved. v200 pre-build compile-risk audit (tick-546) verified the v183-v199 chain compile-coherent in both arity and cbuffer-layout classes. v199 confirmed siblings (`TestRTReflections.cpp`, `TestRenderSponza.cpp`) clean by set-difference.
  - **Build verification**: `./Build.sh --Config=Debug --Target=TestCornellBoxGI --Rebuild && HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 ./TestCornellBoxGI` is needed to confirm the patch does not regress the control. Cannot be done from this runspace (`terminal` denied by tirith per `tools/approval.py:2999-3012`). The cycle produces the patch; the operator verifies per the canonical recipe.
  - **Single-profile caveat**: this is a planning role on a single-profile host, so planner and impler are "same head with different prompt text." The plan-criticer's value is the structural audit (does the helper preserve init-equivalence? does the resize branch re-clear the binding cache?), not fresh-eyes.
  - **Free-static vs member helper**: chose **free static** for the helper because it has no need for `this` (only the device pointer and the output struct). No header change required.

## Plan Deviations
None yet (plan is fresh; this is the FIX revision incorporating the plan-criticer's 5 corrections).