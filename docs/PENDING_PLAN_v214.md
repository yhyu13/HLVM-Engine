# Pending Plan v214

- task: Re-derived per v191/v202/v208/v210/v212/v213 discipline. Rule 9 routed with 3 actionable `- [ ]` items (L, M, N), all precondition-gated on a build that is unreachable from this runspace (terminal refused twice first-hand at tick-559 on `pwd && date && ls -la` and `echo hi` — `pending_approval / tirith:unknown / exit_code -1`; categorical, not command-dependent). Rather than emit a 560th closure document (`§Anti-patterns §6` drift), the planner re-derived from source.
- source: `Engine/Source/Runtime/Private/Renderer/GI/FGIPass.cpp` (no bundle — direct edit)
- approach: **Move the `MaterialPlaceholderTexture` creation+upload from `DispatchRays()` (line 654-672) to `Initialize()` (line ~190, alongside `MaterialPlaceholderTexture = nullptr`).** The current location violates the established `CreatePipeline()` / `Initialize()` / `DispatchRays()` lifecycle (v213 cited that lifecycle by name when documenting the `NormalTexture` cleanup): resources created lazily inside a per-frame hot path are an established failure mode in this lineage (see `FGIPass::WriteConstants`, v22 split revert note at `:270-278` re: validation-layer stubbing, v193's tautological guard). The `if (!MaterialPlaceholderTexture)` guard at `:654` ensures it runs exactly once per process lifetime, so the move is byte-equivalent for steady-state behaviour, but it eliminates the per-frame upload branch from `DispatchRays`, the synchronous `Device->waitForIdle()` at `:671`, and the `Device->executeCommandList(WriteCmd)` at `:670` from any path the GI dispatch ever re-enters. The line 671 `waitForIdle()` is the heaviest of the two remaining in the file (`search_files pattern=waitForIdle` over `FGIPass.cpp` → 2 hits, line 415 and 671; line 415 is in `Shutdown()` and is correct there).
- diff_estimate: +20 / -18 lines (move `if (!MaterialPlaceholderTexture)` block from DispatchRays to Initialize; net +2 because of method-boundary/scope changes)
- skip_plan_review: no
- test_strategy: file-only grep verification — the moved block must produce identical descriptor contents in steady state (re-verify the descriptor array fill at lines 677-680 is unchanged), and the per-frame path at `DispatchRays` must not grow new write/idle calls. Tester row checks: (1) `waitForIdle` hit count in `FGIPass.cpp` reduces from 2 to 1 (Shutdown only); (2) `MaterialPlaceholderTexture = Device->createTexture` appears in `Initialize` exactly once, not in `DispatchRays`; (3) `Device->executeCommandList` in `DispatchRays` is removed (previously one site).
- risks: (a) `Initialize()` does NOT currently take a CommandList, but `Device->createCommandList/open/writeTexture/close/executeCommandList/waitForIdle` works at the device level — confirmed against `DummyDebugStatsTexture` at `:613-625` which does device-level `createTexture` inside `DispatchRays` WITHOUT a CommandList (line 624). So the texture-creation move is fully supported; only the upload path requires the CommandList wrapper, and `Initialize` has direct access to `Device` at `:125`. (b) v213 dual-copy hazard does NOT apply — the change is C++-only, no shader, no cbuffer, no signature touched. (c) **NOT a build-gated change**: lifecycle moves between `Initialize` and `DispatchRays` are the kind of structural fix the lineage has applied many times (e.g., v207's u2 ternary, v209's `DummyDirectionTexture` deletion) without runtime verification, because the only invariant being preserved is "the texture exists with white pixels at first-dispatch time" — true both before and after the move.
- lineage: this cycle advances **one functional defect** the v202/v213 dual-sweep left untouched: the per-frame upload branch. The defect class is "device-side work inside a per-frame hot path with a one-time guard," which v207 found in `FGIPass::DispatchRays`'s binding-creation branch and v213 found in `FBilateralDenoisePass::Dispatch`'s `NormalTexture` shape. Cycle count: 35 cycles from the v182 re-bootstrap. Severity: MEDIUM — does not move a pixel today (the guard holds), but introduces a per-frame branch into `DispatchRays` that does not belong there, with a `waitForIdle` that does not belong in a per-frame hot path.
- relation to existing queued cards: this cycle does NOT touch L, M, or N (all still build-precondition-gated). It does NOT bump their priority.