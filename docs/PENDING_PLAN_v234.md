# Pending Plan v234 — Provenance wrap of v233-tagged source edits

- task: Wrap the 7 v233-tagged source edits that exist on disk but were never documented in any cycle marker as a formal v234 cycle. The edits are functionally consistent with v232 (W reservoir family of fixes): Jacobian clamp (firefly suppression via J bounds), prev-frame normal rotation (turntable correctness), W-clamp-at-source (grazing-angle pdf → 0 fix), and spatial anti-firefly clamp (defensive secondary). Provenance is recovered from inline `// v233:` markers in the source.
- source: no bundle — direct edit of `docs/` markers only (PENDING_*_v234.md and this PLAN). The 7 source edits already exist on disk; v234 does NOT touch HLSL.
- approach:
  1. **Inventory the v233-tagged edits first-hand** via `search_files pattern="v233"` in the three shader files. Confirmed counts (re-verify this turn):
     - `ReSTIR_Temporal_cs.hlsl`: 3 hits (lines 183-187 Jacobian clamp, 254-260 NEW `RotatePrevToCurr`, 310-313 prev-frame normal rotation, 499 r_prev normal rotation). Actually: 3 v233-tagged comments + the `RotatePrevToCurr` function definition at 254-260 is un-tagged but is a v233 add (8 functional edits total).
     - `ReSTIR_Spatial_cs.hlsl`: 2 hits (line 129 Jacobian clamp, line 432 anti-firefly clamp on final estimate).
     - `ReSTIR_Generate_cs.hlsl`: 1 hit (line 110 W-clamp at the source, grazing-angle fix).
  2. **Categorize the edits** into 3 functional groups:
     - **Group A — Jacobian clamp (firefly suppression)**: temporal line 187, spatial line 131. J clamped to `[1e-4f, 1e2f]` (ZetaRay's commented-out line). Bounds the J term in `denom += r_prev[p].M * J * targetLum_prev` so a single bad sample can't dominate the next frame's accumulator.
     - **Group B — Prev-frame normal rotation (turntable correctness)**: temporal lines 252-260 (new function `RotatePrevToCurr`), 306 (call site for position), 312-313 (call site for normal), 499 (rotation of `r_prev[0].normal` at the temporal merge). All use a rigid Y rotation matrix derived from `SceneYaw - PrevSceneYaw`. Necessary because the turntable rotates the scene rigidly and the prev-frame GBuffer was stored in old world coords.
     - **Group C — W clamp at source / spatial anti-firefly (defensive)**: generate line 116 (W = min(1/pdf, 256.0f) at the source, derives w_sum from clamped W), spatial line 432-449 (anti-firefly clamp on the FINAL estimate, same 25x-waveAvg threshold as SuppressOutlierReservoirs with absolute floor of 1.0).
  3. **Document provenance** in PENDING_PLAN_v234.md (this file), PENDING_COMMIT_v234.md, and PENDING_TESTS_v234.md. No new code. No new HLSL.
  4. **Honest re-audit of v233 verifier row 1**: the v233 PENDING_TESTS_v233.md claims `_OPERATOR_RECIPE_v176.sh` exists at the repo root (row 1 PASS). First-hand this turn: 0 hits for `_OPERATOR_RECIPE_v176.sh` across the repo. The canonical recipe at `Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` exists and works standalone (its `--help` documents `bash Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` without a shim), so the row-1 PASS claim is technically a "stale evidence" issue — the shim is missing but the operator can run the canonical recipe directly. Record this in v234 audit so future audits don't propagate the inaccuracy.
- diff_estimate: +0 / -0 HLSL lines (no source changes; provenance documentation only). +6 marker files (`PENDING_PLAN_v234.md` = this file, `PENDING_COMMIT_v234.md`, `PENDING_PLAN_REVIEW_v234.md`, `PENDING_IMPL_REVIEW_v234.md`, `PENDING_TESTS_v234.md`, `PENDING_TEST_AUDIT_v234.md`) + 1 PIPELINE_HEALTH audit (this turn's). Total +0 functional lines, qualifies as trivial per `six-role-pipeline §Anti-patterns §5` ONLY when the patch surface is already on disk — which is exactly the case here. The pipeline overhead is justified because (a) provenance is a real gap (PICK line 14 flagged this), (b) the audit trail is itself a deliverable per user instruction "Keep a concise append-only docs/PIPELINE_HEALTH_YYYY-MM-DD.md audit".
- skip_plan_review: no — second-head review is warranted because the v233 verifier row-1 inaccuracy needs a plan-criticer's judgment on whether to formally correct v233 or just document the divergence in v234. Both choices have different downstream consequences (correcting v233 = modifying a CLOSED cycle; documenting in v234 = cleaner audit trail).
- test_strategy: tester role #5 runs a 12-row file-only verifier confirming:
  - (1-3) `// v233:` count in each of the 3 HLSL files matches the expected site count (3 in temporal, 2 in spatial, 1 in generate).
  - (4) `clamp(j, 1e-4f, 1e2f)` present in temporal (Jacobian clamp Group A).
  - (5) `RotatePrevToCurr` function defined at temporal lines 254-260.
  - (6) `RotatePrevToCurr(gPrevNormals...)` present at temporal line 312.
  - (7) `RotatePrevToCurr(r_prev[i].normal)` present at temporal line 499.
  - (8) `min(1.0f / max(pdf, 1e-6f), 256.0f)` present at generate line 116.
  - (9) `clamp(..., 1e-4f, 1e2f)` present at spatial line 131.
  - (10) Anti-firefly clamp block present at spatial lines 432-449 (`WaveActiveSum`, `25.0f * waveAvg`).
  - (11) `v176-recipe.sh` exists at canonical path and is 489 lines.
  - (12) `validate_restir_gi.py` exists and has `check_black_ratio`/`check_color_variance`/`check_temporal_stability`/`check_cell_variance`.
  testing-verifier role #6 audits for the 5 broken-test patterns + the v233 row-1 inaccuracy.
- risks:
  1. **The 7 source edits may have been intentionally undocumented** (e.g., committed by an operator-side interactive session without cycle wrapping). If so, v234's documentation might be retroactive noise. Mitigation: the v10 audit's PICK line 14 explicitly flagged this as a documentation concern that does not block runtime verification — so the operator already knows and wants provenance recovered. v234 addresses that.
  2. **The Jacobian clamp + spatial anti-firefly clamp could conflict with v232's W-clamp** in pathological cases (over-clamping chain). Mitigation: read the inline comments: v232's W-clamp is at the W-compute-site (after Jacobian); Group A's J-clamp bounds the input; Group C's spatial anti-firefly is on the FINAL estimate (post-W-compute). The chain is mathematically consistent (J bounded → w_sum bounded → W bounded → estimate bounded).
  3. **The Cornell copies may also have v233-tagged edits.** First-hand check this turn: search for v233 in `TestCornellBoxGI_Data/` returns 0 hits. Cornell algorithm is simpler and doesn't need these fixes (matches v232's deviation).
  4. **v234 is a documentation-only cycle, not a code change.** Per `six-role-pipeline §Anti-patterns §5`, "use a direct edit with a one-shot verification step" would be faster. The pipeline overhead is justified here ONLY because the user explicitly invoked the skill AND the v233 source edits have no documented provenance (PICK line 14) AND the verifier-row correction requires a multi-role audit (correction of a CLOSED cycle vs. documenting-in-v234 is a non-trivial choice).
  5. **Terminal + vision runspace boundary**: v234 itself does not require terminal/vision (it's documentation-only); the underlying patch (the v232 W-clamp + v233 source edits) requires operator-side runtime verification via `bash Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` which is BLOCKED at the terminal boundary. v234 produces the audit trail; the runtime gates remain HUMAN_REQUIRED.
- relation to existing queued cards: this cycle does NOT touch PICK line 5 `[x]` (the original GBuffer SRV binding fix card). It addresses PICK line 14 (v233-tagged source edits out-of-scope) by giving those edits a formal cycle. After v234 closes, PICK line 14 can be marked `[x]` and the queue is at 1 actionable item (the recipe-broken card on line 12, which is operator-side: either re-create `_OPERATOR_RECIPE_v176.sh` or update the shim's reference path, or run the canonical recipe directly).
- relation to v232 + v233 markers: v234 documents that v232 + v233 (the latter being the source-edit provenance gap) constitute the complete load-bearing fix. After v234 closes, the patch surface is:
  - v232: 4 temporal W-clamp sites + 1 spatial W-clamp site (load-bearing: breaks cross-frame feedback loop).
  - v233: 7 source-file edits across 3 HLSL files (load-bearing: Jacobian clamp + turntable rotation + W-clamp-at-source + spatial anti-firefly clamp).
  - Operator-side runtime verification: `bash Engine/Source/Runtime/Test/TestReSTIR_GI_Temporal_Data/v176-recipe.sh` (489 lines, all 7 gates).
