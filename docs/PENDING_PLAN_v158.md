# Pending Plan v158
- task: TestReSTIR_GI_Temporal acceptance verification — file-only cycle-stop re-affirmation with handle-identity evidence
- source: no bundle — verification-only cycle against the existing v137, v140, and v151 fixes
- approach: Re-affirm the v157 verification-only plan with one additional on-disk evidence channel that v157 did not exercise: direct read_file of the 2026-08-08 17:30:49 fresh non-bypass log to compare GBufferMaterial/WorldPos/Normal handle-IDs between `RenderGBuffer` and `FGIPass::DispatchRays`. The 2026-07-30 diagnostic's mode-20 zero result is hypothesis #4 ("handles stale/different") which becomes FALSIFIED if the handles match across the boundary. The 17:30 log shows the handles match exactly (Material=0x3cbc40c9300, WorldPos=0x3cbc40c6040, Normal=0x3cbc40c8c00) in both calls. The mode-20 zero result was authored BEFORE v137; v137 is intact on disk (`FGIPass.cpp:289 SetBindingOffsets(0,0,0,0)`). No production edit authorized; the next required runtime evidence (terminal+vision+python3+numpy) cannot be produced in this runspace.
- diff_estimate: +0 / -0 production lines for verification; any evidence-driven fix requires a subsequent reviewed plan
- skip_plan_review: no
- test_strategy: Build `TestReSTIR_GI_Temporal` in Debug; run the non-bypass target with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; run a mode-20 discriminator with the same settings; execute `validate_restir_gi.py` against the newest dump group only; use numpy per-pixel/channel statistics and visual review of the newest `*_display_frame8.png`; require mode 20 to be non-zero and spatially varying and require fresh logs to contain no Vulkan VUID/ERROR or command-list errors.
- risks: This scheduled runspace's terminal request is blocked again this tick with `status: pending_approval` / `pattern_key: tirith:unknown` for `date; pwd; ls; python3 -c` and all earlier probes. No vision tool is registered. The handle-identity evidence this tick is a new on-disk signal that strengthens the v137 hypothesis: the 2026-07-30 mode-20 zero cannot be reproduced by hypothesis #4 (handles differ) because the handles match. The next non-handle hypotheses to bisect would be (1) slangc dead-strip on the debug-mode switch, (2) image layout transition mismatches, (3) nvrhi second-binding-set drop. None are addressable from a file-only runspace. Do not modify production code, mark the PICK card complete, or claim validator/vision success without fresh execution evidence.
