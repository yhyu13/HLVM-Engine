# Pending Plan v150
- task: TestReSTIR_GI_Temporal ReSTIR reservoir accumulation fix
- source: no bundle — direct edit
- approach: Bisect why the ReSTIR reservoir output is all-zero (radA/MW_A/radB/MW_B = 0.0, M=0.00 max=0.0) despite gi_raw being non-uniform. Two sub-bugs suspected: (1) Generate pass writes reservoir textures but a layout/transition ordering issue causes the Temporal pass to read zero; (2) the Generate pass itself produces zero (e.g., the screen-space reservoir sample code never accumulates). The 2026-08-05 15:43 log shows gi_raw ≠ 0 (std=0.78) but ReSTIR summary M=0 — so the Generate pass is reading gi_raw as zero or never writing. The bug-075 binding-layout split (SRV-only set 0 + UAV-only set 1) was applied to the temporal pass; verify the Generate pass binding layout is similarly split, and verify the Generate pass's SRV reads of gi_raw / GBufferWorldPos / GBufferNormal actually see the data the GI shader wrote (not the sentinel that bit HLVM-Engine per `software-development-practices §Sentinels-then-overwrite`).
- diff_estimate: +50 / -50 lines
- skip_plan_review: no
- test_strategy: Build Debug target; run HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 with HLVM_RGI_BYPASS=0 (so ReSTIR runs); inspect reservoir_radA/MW_A/radB/MW_B and spatial/denoised stats for non-zero std; vision-check display PNG for recognizable Sponza with non-flat shading; run validate_restir_gi.py on the fresh dump group; require no VUID/ERROR/command-list errors. If Generate-pass binding layout still uses a single combined layout with both SRV+UAV, split it per the temporal-pass pattern.
- risks: Terminal execution blocked by tirith in cron runspace; the operator runspace must do the build/run/inspect. If sentinel uploads are still active in the test harness, the SRV read in the Generate pass may see sentinels (not the post-raster GBuffer) — remove or env-var-gate sentinels per the methodology. Do not claim completion without fresh runtime evidence.
