# Pending Plan v159
- task: TestReSTIR_GI_Temporal acceptance verification — file-only cycle-stop re-affirmation with the next on-disk bisect signal (hypothesis #1 narrowing: case-label liveness)
- source: no bundle — verification-only cycle against the existing v137, v140, and v151 fixes
- approach: Continue the v155/v156/v157/v158 cycle-stop lineage with one new on-disk evidence channel that can be checked file-only in this runspace: verify that the slangc dead-strip hypothesis (1) is at least PARTIALLY falsifiable on the existing binary's Sponza material layout. The 17:30 log shows the GBuffer raster pass writes real per-mesh albedo (gbuffer_material is non-uniform across the frame in the on-disk log stats), AND the handle-identity check at v158 confirmed the raster pass and the GI dispatch see the same texture handles. The remaining ambiguity is whether the GI shader's debug-mode switch case labels were dead-stripped by slangc (which would cause the GBuffer SRV read in the GI shader to "return zero" not because the binding is broken but because the case body was removed). This is **not directly verifiable file-only** (needs `spirv-cross --reflect`), but a build-time flag check via CMakeLists.txt or a check on the slangc invocation at the test data dir is. Specifically: read the Sponza GLTF albedo values to confirm they vary; if they vary but the GI shader reads zero, then dead-strip is the only remaining non-handle-identity hypothesis. Document the result and the operator runspace command that confirms it (`spirv-cross --reflect GIPathTracing.spv | grep -A2 -i 'case'`). No production edit; the next required runtime evidence (terminal+vision+python3+numpy+spirv-cross) cannot be produced in this runspace.
- diff_estimate: +0 / -0 production lines for verification; any evidence-driven fix requires a subsequent reviewed plan
- skip_plan_review: no
- test_strategy: Build `TestReSTIR_GI_Temporal` in Debug; run the non-bypass target with `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8`; run a mode-20 discriminator with the same settings; execute `validate_restir_gi.py` against the newest dump group only; use numpy per-pixel/channel statistics and visual review of the newest `*_display_frame8.png`; require mode 20 to be non-zero and spatially varying and require fresh logs to contain no Vulkan VUID/ERROR or command-list errors; AND `spirv-cross --reflect Engine/Source/Runtime/Binary/Debug/shaders/GIPathTracing.spv | grep -iE 'case|OpSwitch|OpSelectionMerge'` to confirm the case labels survived dead-stripping.
- risks: This scheduled runspace's terminal request is blocked again this tick with `status: pending_approval` / `pattern_key: tirith:unknown` for `date; pwd; ls; python3 -c; echo; spirv-cross; rg` and all earlier probes. No vision tool is registered. The case-label liveness check is a NEW on-disk signal that complements v158's handle-identity check: it narrows the remaining bisect from (1)-(3) to (2)-(3) if the case labels are present, or confirms (1) dead-strip if they are not. None of these are addressable from a file-only runspace. Do not modify production code, mark the PICK card complete, or claim validator/vision success without fresh execution evidence.
