# Pending Plan v224

- task: Fix the stale `verify:` line in `docs/PENDING_COMMIT_v214.md:10` that produces a false failure on a correct tree. Card T from PENDING_PICK.md. **State the operator-facing action** so that running `verify` returns the right answer.
- source: no bundle — direct edit of one marker file (`docs/PENDING_COMMIT_v214.md`)
- approach: card T offers a real choice between (a) correcting the verify line in place and (b) superseding it with a correction note. **Plan: option (b) — supersede**. Reasoning: the state machine reads the marker chain (PENDING_COMMIT → PENDING_TESTS → PENDING_TEST_AUDIT) to verify a cycle closed correctly; rewriting a closed cycle's `verify:` line in place would silently change what the audit chain claims happened, which is a deeper integrity issue than a stale number. A correction note preserves the original audit trail while giving an operator a working verify. Implementation: append a `## Verify-line correction` section to `docs/PENDING_COMMIT_v214.md` (before the `## Plan Deviations` section so the append-only invariant of the marker chain is preserved), stating the actual post-state, the cause of the drift (v214 itself renamed/moved the Shutdown `waitForIdle` from `:415` to `:441` and a comment hit at `:177` was never accounted for), and the corrected verify command.
- diff_estimate: +12 / -0 lines (one marker file, no source touched)
- skip_plan_review: no
- test_strategy: role #5 (tester) re-derives the actual `waitForIdle` count and lines in `FGIPass.cpp` independently; role #6 (verifier) audits the correction note against v198 ("never let a marker's diff-size claim rest on intent rather than on the returned diff") and v199 ("counts are invariants, sets are") — the corrected note must name the LINE NUMBERS not the count alone, because the count has held at 3 across cycles but the line numbers drifted.
- risks:
  - **Stale-line risk in the correction note itself.** v217 established that counts survive across cycles but line numbers do not. The corrected verify must name the symbols (`waitForIdle`, `MaterialPlaceholderTexture`, `executeCommandList`) and the structural location (Initialize, DispatchRays, Shutdown), not the line numbers. The note can cite the *current* lines (`:177` comment, `:197` Initialize, `:441` Shutdown) as observed-this-turn but should explicitly say "see source for live numbers" so a future editor does not cite this note's numbers as authoritative after another refactor.
  - **Append-only invariant.** The marker chain's append-only discipline (per HARD INVARIANT) means edits go AFTER existing sections, never modifying prior text. The plan uses `patch` with `old_string` anchored on the literal phrase "## Plan Deviations" and inserts a new section BEFORE it (the patch tool inserts AFTER the matched `old_string` line if `new_string` contains a trailing newline, or BEFORE if anchored differently — verify the returned diff).
  - **State machine integrity.** Adding a section to a closed cycle's commit marker does not change the cycle's verdict (KEEP) or its audit trail's verifiability (the marker chain is still readable end-to-end). The test/audit markers for v214 remain as they were. The supersede note must explicitly say "this correction does NOT change v214's verdict or audit trail."
  - **Anti-condition check.** Same as v223: (i) interactive GPU debug — NO (marker edit, no code path); (ii) single-line surgical patch — NO (12-line append, structural note); (iii) single-profile file-only host with terminal blocked — irrelevant. None apply; the skill is appropriate.

## What this plan does NOT do
- Does NOT modify `Engine/Source/Runtime/Private/Renderer/GI/FGIPass.cpp` or any source file.
- Does NOT modify `docs/PENDING_PLAN_v214.md`, `docs/PENDING_PLAN_REVIEW_v214.md`, `docs/PENDING_TESTS_v214.md`, or `docs/PENDING_TEST_AUDIT_v214.md`.
- Does NOT change v214's verdict (KEEP) or its audit trail.
- Does NOT modify `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, or any governance file.