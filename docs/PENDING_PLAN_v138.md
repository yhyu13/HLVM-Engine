# Pending Plan v138
- task: Add `6u` to `bypassEarlyReturn` debug-mode list in GIPathTracing.hlsl so mode 6 actually discriminates UAV-bug vs SRV-bug (tick 248's discriminator was masked by the early-return at line 481)
- source: docs/DIAGNOSTIC_2026-07-30-v24.md §"What mode 6 reveals" (claims mode 6 was a no-SRV gradient discriminator, but the early-return at line 481 clobbers its output for every pixel when GBufferWorldPos SRV returns zero) + docs/PIPELINE_HEALTH_2026-07-31_tick248.md (incorrectly identified mode 6 as the post-v137 discriminator) + this tick (tick 250) re-read of GIPathTracing.hlsl:460-484
- approach: At `Engine/Source/Runtime/Private/Renderer/Shader/GI/GIPathTracing.hlsl:475-479`, the `bypassEarlyReturn` boolean only includes modes 20u/21u/22u/30u/31u. Mode 6 (added at v13 on 2026-07-27) writes a per-pixel gradient to `OutputTexture` at line 608, but the early-return at line 481-484 fires when `length(worldPos) < 0.001` (i.e. when `GBufferWorldPos[pixel]` SRV returns zero — the very bug being diagnosed). When the early-return fires, `Output[pixel] = (0,0,0,1); return;` runs BEFORE the debug-mode switch at line 593, so mode 6's gradient write at line 608 never executes. Mode 6's "no-SRV gradient" diagnostic is structurally MASKED by the early-return whenever the SRV bug is present. **Tick 248's reasoning that "mode 6 is the FIRST discriminator post-v137" was incorrect** — mode 6 doesn't bypass the early-return. Fix: add `6u` to the `bypassEarlyReturn` list. Then mode 6 will write its gradient to `OutputTexture` REGARDLESS of whether `GBufferWorldPos` returns zero, allowing mode 6 to actually discriminate "is the UAV write broken" (mode 6 dump = zero → UAV bug) vs "is the SRV read broken" (mode 6 dump = gradient → SRV bug, v137 was a wrong-fix). This matches the v128/v131 design intent (modes 20/21/22/30/31u already in the bypass list for exactly the same reason — the diagnostic needs to run regardless of whether the GBufferWorldPos SRV is healthy).
- diff_estimate: +1 / -0 lines (one new entry in the `bypassEarlyReturn` chain at GIPathTracing.hlsl:478)
- skip_plan_review: no (this is a 1-line surgical patch but the reasoning chain is novel and counter-intuitive — plan-criticer should verify the early-return really fires for mode 6, and that the addition doesn't break any other mode)
- test_strategy: role #5 (tester) verifies file-only state: (a) `6u` is added to `bypassEarlyReturn` chain at GIPathTracing.hlsl:478, (b) no other lines affected, (c) v131+v135+v136+v137 patches intact, (d) shader recompile will pick up the change
- risks:
  1. **Mode 6 may now show non-uniform color even when the bug is present** — this is the GOAL, not a risk. If mode 6 was returning zero ONLY because of the early-return mask, adding it to the bypass should cause mode 6 to actually run. The shader body for mode 6 (line 608) writes `float3(float(pixel.x)/256.0, 0.0, float(pixel.y)/256.0)` which is a per-pixel gradient (red on x-axis, blue on y-axis, green=0). Expected dump: visible gradient.
  2. **Mode 6 may now produce garbage that looks like the SRV is broken when it isn't** — risk: low. Mode 6 doesn't read any SRV (it uses `DispatchRaysIndex().xy` which is built-in). The gradient is computed from `pixel.x` and `pixel.y` only. If mode 6 shows the gradient after the fix, the GI shader's UAV write is fine and the only remaining bug is the SRV read (the original v24 diagnostic's hypothesis).
  3. **The fix may interact with slangc dead-strip** — risk: very low. Mode 6's case body uses `pixel.x` and `pixel.y` which slangc cannot dead-strip (they're always read). The bypass list addition only adds a comparison branch; no shader body change.
  4. **The fix may break slangc compilation** — risk: very low. The bypass list is already a chain of `||` comparisons on an integer; adding `|| debugModeEarly == 6u` is structurally identical to the existing entries.
  5. **The fix doesn't address the v137 UAV-binding bug separately** — risk: low. The v137 fix is still correct on its own merits (descriptor writes at slot 768 vs shader expecting 384 — both v24 diagnostic and tick 248 confirmed this is a real bug in the descriptor layout, even if it wasn't the root cause of the all-zero gi_raw symptom). The fix doesn't revert v137; both fixes can coexist.
  6. **The fix doesn't directly fix the all-zero gi_raw symptom** — risk: medium. This is a DIAGNOSTIC patch, not a behavioral fix. After v138, mode 6 should produce the gradient; if it does, then the root cause is the SRV binding (which is NOT fixed by either v137 or v138). v139 will need to address the SRV binding. **Mitigation**: the parent runspace can now discriminate UAV vs SRV; if SRV is the bug, v139 investigates (a) image layout transition for first-frame reads (v135 may have missed the GBuffer→SRV transition specifically), (b) descriptor-set creation flags (e.g. PARTIALLY_BOUND allowing writes to undefined slots), (c) slangc dead-strip (falsified by v24 spirv-dis but worth re-checking after v138 if mode 6 also returns zero), (d) nvrhi's auto-barrier ordering for the SRV binding layout (the SRV layout's `requireTextureState(SHADER_READ_ONLY_OPTIMAL)` calls may be missing for the GBuffer textures after the raster pass transitions them to `ShaderResource`).

**Concrete discrimination recipe after v138** (parent runspace, file-only verifiable plan):
1. Build: `./Build.sh --Rebuild --Target=TestReSTIR_GI_Temporal --Config=Debug` (terminal required)
2. Run mode 6: `HLVM_DUMP_RGI=1 HLVM_RGI_ACCUM=8 HLVM_PT_DEBUG_MODE=6 ./Binary/Debug/TestReSTIR_GI_Temporal`
3. Inspect `dumps/<timestamp>_gi_raw_frame8.png` with numpy per-channel stats.
4. **Decision tree**:
   - **Mode 6 dump shows per-pixel gradient** (red on x-axis from 0..1, blue on y-axis from 0..1, green=0): UAV bug WAS the only one. v137 fixed it. Mode 20/21/22 should ALSO now return non-zero (they were masked by the same early-return). Run mode 20 to confirm. Then run validate_restir_gi.py. Bisect closes if validate passes.
   - **Mode 6 dump still all-zero**: SRV bug is present. v137 was a wrong-fix for the symptom. v139 needs to investigate the SRV binding. Possible v139 directions: (a) check the v135 commitBarriers actually fires for the GBuffer→SRV transition (may need an explicit `cmdList->setTextureState(GBufferWorldPos, SHADER_READ_ONLY_OPTIMAL)` before createBindingSet), (b) check nvrhi's auto-barrier list to see if GBuffer textures are transitioned before the SRV read, (c) enable Vulkan validation layer explicitly via `VK_LAYER_KHRONOS_validation=1` env var to get a precise error.

**Why this is the right next step**:
- v137's premise (mode 6 would discriminate UAV vs SRV after v137) is structurally wrong because mode 6 is masked by the early-return.
- v138's fix (adding mode 6 to bypass) restores the discriminator that v17/v18 originally designed.
- The fix is 1 line, low-risk, and unblocks the next-level bisect.
- The fix is independent of v137 — both can coexist; v137 may still be a real bug fix for future paths, but it wasn't the root cause of the symptom.

**Per `software-development-practices §Plan Mode`, this plan is delivered as a markdown file. No file edits performed by this role.**