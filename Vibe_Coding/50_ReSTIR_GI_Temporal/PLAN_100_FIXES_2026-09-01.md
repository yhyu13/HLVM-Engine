# PLAN — 100 Fixes for the ReSTIR GI Line (P0 → P2), 2026-09-01

Scope: `TestReSTIR_GI_Temporal` + its data dir (shaders, validator, dumps),
`TestCornellBoxGI` where shared, and the `Vibe_Coding/50_*`/`51_*` docs.
Goal anchor: a ReSTIR GI path tracer that is real-time AND verifiably
correct on real content — every fix below serves that; anything else is P2
hygiene and explicitly labeled.

Status legend: `[ ]` open, `[x]` done (with commit/date). P0 = correctness
or blocking; P1 = quality/robustness; P2 = hygiene/nice-to-have.
Each item is mechanically actionable: file + what + how to verify.

---

## P0 — Correctness / blocking (10)

0. `[ ]` **NEW #1 (supersedes the v236 narrative): the small-estimate
   population.** 32–65% of gi_raw-lit pixels carry a spatial estimate far
   below raw (float means 0.039 vs 0.104); regionally structured (one side
   of the screen worse); NOT clamps/M-depth/spatial-merge/compounding/
   history/invalid-candidates/GBuffer-degeneracy (all ruled out by the E1–G2
   bisect, FIX_LOG_2026-08-26 addendum v236 REVISED). The v236 "sky-bounce"
   root cause is RETRACTED. First probe: float readback (CPU map, not PNG —
   quantization confounds everything < 1/255) of FullResSpatial + generate
   reservoirs on a 64x64 crop spanning the healthy/unhealthy boundary;
   compare targetZ, W, w_sum per pixel. Verify: the population shrinks to
   genuinely-dark pixels; valid-domain and full-domain bias converge.
1. `[x]` "Bias 0.06 starfield" gate-side fixed: bias gate moved to the valid
   domain (the estimator is unbiased where it produces output — 0.951/0.811).
   DONE 2026-09-01, `b1aac41` + REVISED addendum. The render-side cause is
   item 0, still open.
2. `[x]` Validator bias gate measured a design-unexplained population — moved
   to the valid-reservoir domain. DONE 2026-09-01, `b1aac41`.
3. `[x]` Noise gate premise unmeasurable (raw estimate near-deterministic) —
   replaced with relative-HF noise on the valid domain. DONE, `b1aac41`.
4. `[ ]` **Redo the Sponza ReSTIR-vs-path-trace converged comparison**
   (BLOCKED on item 0 — a comparison against a half-missing estimate is
   meaningless).
   (256f both, fixed camera): the v215 "5.4%" is void. Verify: rel err
   reported with sky-bounce fraction stated; expect single-digit %.
5. `[ ]` **ReBLUR hit-dist behavior on real content**: v234's alpha change
   was calibrated on a wall; valid-domain denoised mean (0.062) vs gi_raw
   (0.100) suggests ReBLUR params (`HitDistParams.D=-25`, NormRatio) need
   recalibration for atrium distances. Verify: HF-noise ratio improves and
   denoised/spatial mean ratio → 1.
6. `[ ]` **Static display fireflies** (upper wall speckle, firefly value
   16.3). CRITIC-REVISED: the wave clamp's active-lane division LOOSENS the
   cap in sparse waves (understated average → lower cap → more clamping), so
   it cannot be the 16.3's cause. First locate the artifact's source (u4
   primary-NEE direct is unclamped — prime suspect), then fix at the source.
   Verify: firefly bound < 10 with the clamp untouched.
7. `[ ]` **Lo-content audit (CRITIC-REVISED premise)**: both engines store
   multi-bounce Lo (ZetaRay `PathTrace` accumulates NEE up to
   maxNumBounces=3 with stochastic depth halving). The REAL divergences to
   audit: (a) HLVM's Miss shader adds sky into payload.radiance so a VALID
   reservoir's Lo can contain sky, while ZetaRay breaks on miss; (b)
   STOCHASTIC_MULTI_BOUNCE not ported. Document both; fix only if a metric
   moves. Verify: Cornell 1024f vs PT (≤8%).
8. `[ ]` **Validator unit test for bias/noise domain change** (v236): the
   SceneContentGateTests cover content only; add synthetic valid/invalid
   mask tests for `check_bias_bound` and `check_noise_reduction`. Verify:
   `python3 test_validate_restir_gi.py` green with the new cases.
9. `[ ]` **Commit-hygiene gate for dumps** (CRITIC: mostly done —
   .gitignore already covers both dumps dirs, 0 tracked PNGs). Remaining:
   pre-commit hook rejecting `git add -f` of dump PNGs, and a lint that new
   `*_Data` dirs get a dumps/ ignore entry automatically.
10. `[ ]` **Debug-binary (validation ON) full pass of the 3 configs** after
    v236: Debug runs for static/rotating/Cornell with 0 VUIDs. Verify: logs
    archived in `evidence/v236_skybounce_fix/`.

## P1 — Quality / robustness (30)

11. `[ ]` ReBLUR HistoryFadeIn/temporal alpha audit on real content (the
    08-09 calibration was wall-era): sweep `HitDistParams` on static,
    gate = HF-noise ratio.
12. `[ ]` Spatial radius 3 → parameterize via CVar (`r_ReSTIR_SpatialRadius`),
    sweep {3,5,8} on static+rotating, gate = bias/HF + frame time.
13. `[ ]` NormalThreshold 0.9 / DepthThreshold 0.05: sweep on real content,
    record acceptance-rate vs bias (dump a rejection-count debug channel).
14. `[ ]` Spatial `numSamples` hardcoded 2 (`InitPairwiseMIS(2.0f, r)`):
    CVar + sweep; ZetaRay caps at 2 — document why.
15. `[ ]` `k_MaxPlaneDistReuse = 0.005` — re-derive for the 0.01-scaled
    Sponza (wall-era constant); verify no over-rejection at grazing walls.
16. `[ ]` Turntable reprojection: verify candidate validity vs rotation
    speed (re-run the v213 0.18°/frame threshold on the fixed camera).
17. `[ ]` Cornell 1024-frame converged ReSTIR vs PT with v236 (the v215 7.4%
    predates the sky/valid-domain changes); refresh the number.
18. `[ ]` Sun-only vs sun+sky ablation of gi_raw vs reservoir (quantify how
    much indirect is NEE-carryable vs reservoir-only).
19. `[ ]` `firstSampleLo` multi-sample (spp>1) path: spp is 1 in all runs —
    either wire spp>1 correctly into RIS or hard-assert spp==1.
20. `[ ]` `r_M` reset policy on history validity: audit `M_new` accumulation
    when `candidate[k].valid == false` mid-pair (ZetaRay parity).
21. `[ ]` Sky-bounce energy double-count audit: primaryMiss AND bounceSkyMiss
    are mutually exclusive by construction — add a debug-mode assert.
22. `[ ]` `GIAccumulate` display = u4 + denoised: verify no energy double-
    count when the reservoir is VALID but its Lo includes sky-lit direct.
23. `[ ]` Replace the hardcoded "(MaxM=10.0)" format arg test — assert the
    logged cap equals the CVar in a unit test (v235 regression guard).
24. `[ ]` Validator: gate on denoised-vs-spatial mean ratio (denoiser must
    be energy-preserving ±10%) — currently unchecked.
25. `[ ]` Validator: `--min-frame` flag so a chosen frame (not just newest)
    can be validated (bisect-friendly).
26. `[ ]` Validator: emit JSON output (`--json`) for the overseer pipelines.
27. `[ ]` `dump_pixelstats.py` rewrite lost per-channel stats — restore
    R/G/B percentile output used by the 08-23 calibration.
28. `[ ]` Evidence hygiene: every FIX_LOG claim links an evidence file that
    exists — add a doc-lint script (grep evidence paths, `test -f`).
29. `[ ]` Recipe scripts (v173/v176/v2) reference stale binary paths —
    regenerate for v236 and delete the stale ones.
30. `[ ]` `README.md` in TestReSTIR_GI_Temporal_Data: document the v236
    valid-domain gate semantics and sky_bounce_frac.
31. `[ ]` TestCornellBoxGI shares ReSTIR shaders — sync the v236 GIPathTracing
    sky-bounce fix into its data dir (or factor a shared source).
32. `[ ]` ShaderMake rebuild determinism: verify identical .hlsl → identical
    .sblob (byte-compare two rebuilds); if not, document the non-determinism.
33. `[ ]` Env-var docs: `HLVM_RGI_*` family consolidated into one table in
    the test header comment (some knobs only exist in git history).
34. `[ ]` Log a per-run camera line (pos/target/yaw/fov) so evidence PNGs are
    self-describing (would have caught the wall incident in the log alone).
35. `[ ]` Add the scene AABB to the run log (v235 measured it by hand).
36. `[ ]` `HLVM_RGI_MAXM` test: unit-test the env override actually clamps
    (E2 showed the cap only clamps the LABEL in some paths — verify M_new).
37. `[ ]` Static-mode spatial: ping-pong parity comment says the spatial pass
    must match temporal parity — add an assert/log when they diverge.
38. `[ ]` ReBLUR denoised alpha: verify the weighted-averaged hitDist is
    finite everywhere (guard NaN in Resolve).
39. `[ ]` Fireflies: add a SuppressOutlierReservoirs-equivalent AFTER the
    temporal merge on the raw estimate path (ZetaRay parity check).
40. `[ ]` Profile: half-res trace vs full-res — measure the actual frame-time
    win to justify the resolve complexity (numbers into the README).

## P2 — Hygiene / nice-to-have (60)

41. `[ ]` Delete stale `old/Card_t_*` handoff cards (superseded by JOURNEY).
42. `[ ]` Merge `claude.md` superseded content into `old/` (backlog says so).
43. `[ ]` `index.html` in Vibe_Coding: link 50/51 journey + fix logs.
44. `[ ]` Rename FIX_LOG_2026-08-26.md → split FIX vs FAIL (it is now both).
45. `[ ]` Add anchors: FIX_LOG sections get ids so evidence links are stable.
46. `[ ]` Convert the 4-check/5-check/9-gate docstring drift in
    validate_restir_gi.py into an auto-generated gate registry.
47. `[ ]` Validator exit codes: document the current set in `--help`.
48. `[ ]` `test_validate_restir_gi.py`: drop the Build/ compiler-cache
    dependency removed in v234 (verify nothing else reads it).
49. `[ ]` Pin numpy/PIL minimum versions in a requirements.txt.
50. `[ ]` .gitignore: add `Engine/Source/Runtime/Test/TestCornellBoxGI_Data/dumps/`
    (done v235) — verify no other test dumps leak.
51. `[ ]` Remove the unused `gi_lo` dump from the default dump set (only
    diagnostics need it) or document its purpose in README.
52. `[ ]` Grep for dead `HLVM_PT_DEBUG_MODE` references post-v17 cleanup.
53. `[ ]` `SampleSky`: hoist constants (zenith/horizon/ground) into a cbuffer
    — currently baked, blocking scene variation.
54. `[ ]` `EstimateDirectLighting` seed: pixelSeed reuse across spp — audit
    correlation (s*7919 stride may alias with 83492791 multiplier).
55. `[ ]` Replace magic `7919u`/`83492791u` RNG constants with named consts.
56. `[ ]` `OffsetRayOrigin` vs ray TMin=3e-6: document the self-intersection
    policy in one place.
57. `[ ]` Move `k_hammersley[8]` to a shared include (duplicated in ZetaRay
    port and possibly elsewhere).
58. `[ ]` `FPairwiseMIS.k` is float; ZetaRay uses uint16 — align type to make
    the port diffable against upstream.
59. `[ ]` Reservoir struct: static_assert offsets vs the 3-texture packing —
    currently only the C++ side asserts sizes.
60. `[ ]` `LoadReservoir`/`StoreReservoir`: assert the OctEncode round-trip
    in a unit shader test.
61. `[ ]` Validator: warn (not fail) when `n_frames==1` — temporal gate is
    vacuous (document in output line).
62. `[ ]` Validator: `check_log_metrics` should FAIL (not skip) when `--log`
    is given but contains no stats lines — silent-skip hid the frame-time
    hole (backlog #4).
63. `[ ]` Emit `frame time: X ms/frame` from the test (enables gate #62).
64. `[ ]` Move `r_ReSTIR_MaxM` comment "(default 10.0f since v215)" into the
    CVar description string so `--help`-style dumps carry it.
65. `[ ]` Test binary: print the git commit hash at startup (evidence logs
    become self-identifying).
66. `[ ]` Dump writer: write a sidecar .json (camera, frame, commit, scene)
    per dump group.
67. `[ ]` Evidence dirs: add a README template (what/why/config per PNG).
68. `[ ]` `JOURNEY.md`: add the v236 era (sky-bounce root cause).
69. `[ ]` `next-session-backlog.md`: prune items resolved by v236 (#1–#3).
70. `[ ]` AGENTS.md RT payloads section: add the slangc dead-strip repro
    command from 51_PathTraceGI_Debug (currently prose only).
71. `[ ]` AGENTS.md: add "sky-bounce candidates are invalid by design" to
    Critical Gotchas (this bug cost 3 sessions).
72. `[ ]` AGENTS.md: add "validators need a semantic content gate" lesson.
73. `[ ]` `.wolf/cerebrum.md`: mirror the three new Do-Not-Repeats (74–76).
74. `[ ]` DNR: never calibrate a gate on a framing you have not looked at.
75. `[ ]` DNR: split the population before chasing a gate number.
76. `[ ]` DNR: an env-override A/B (MaxM=1) beats an hour of shader reading.
77. `[ ]` Validator: cap PNG load memory (800x600 fine; guard 4K dumps).
78. `[ ]` `check_temporal_stability`: also compare per-pixel std (not only
    frame means) — catches flicker with stable means.
79. `[ ]` Validator: check `spatial <= gi_raw` outlier fraction (energy
    creation, not just loss).
80. `[ ]` Docs: one diagram of the v236 data flow (u0–u4, reservoirs, gates).
81. `[ ]` Rename `SpatialRadiance` (it holds the temporal estimate until the
    spatial pass overwrites) — `HalfResEstimate` or similar.
82. `[ ]` Remove the unused `RadianceTexture` binding in the spatial desc
    ("unused in v1 reservoir math") or wire it.
83. `[ ]` Constants structs (`FReSTIRSpatialConstants` etc.): static_assert
    sizes match the shader cbuffer layouts.
84. `[ ]` CVar docs: `r_ReSTIR_MaxM` saved-flag writes to INI — verify the
    INI does not pin a stale 30 from wall-era runs (user-side surprise).
85. `[ ]` Delete the pre-v236 wall-era groups in `dumps/` (513 files, 115
    groups) after archiving the ones referenced by docs.
86. `[ ]` `Build.sh --Test` for TestReSTIR runs headless? Verify and document
    (X11 dependency of the window path).
87. `[ ]` Add a tiny CPU reference for a 64x64 Cornell crop (51 has one for
    PT) to unit-test the reservoir estimate end-to-end.
88. `[ ]` Sweep `AmbientScale` (ambient hack) — with sky-bounce fixed, the
    hack may double-count sky energy; measure and maybe retire it.
89. `[ ]` `sampleHemisphereCosine` vs `GBufferMaterial.a` roughness: document
    that only the Lambert lobe is sampled (specular ignored) in the header.
90. `[ ]` ZetaRay roughness heuristic (`roughnessDiff < 0.2`) is NOT ported
    (Sponza roughness 0.9 constant hides it) — port it for generality.
91. `[ ]` ZetaRay emissive rejection in the spatial neighbor loop — not
    ported; add with a Cornell emissive-light-neighbor test.
92. `[ ]` `PartialReadReservoir_Reuse` (ZetaRay reads prev-frame reservoirs
    for spatial) vs port reading current-frame — document the divergence and
    its bias implications in the shader header.
93. `[ ]` Validator: unit-test `find_group_for_frame` when the same frame
    number exists in 3+ groups.
94. `[ ]` CI-able smoke: `ACCUM=2` run + validator must pass in <60s —
    script it as `smoke_restir.sh`.
95. `[ ]` `docs/`: consolidate the three pending-plan docs from other
    pipelines that reference this test (or mark them foreign).
96. `[ ]` Sweep validator thresholds with the v236 numbers (COLOR_VARIANCE
    0.005 was wall-era; the real floor on real content is 0.06+).
97. `[ ]` Pixel-stats: emit the sky-bounce fraction from the C++ side too
    (it knows the validity mask exactly) — cross-check the PNG proxy.
98. `[ ]` Add `HLVM_RGI_DUMP_EVERY=N` for temporal-gate debugging (currently
    only the last frame is dumped; the temporal gate is vacuous on it).
99. `[ ]` Consider `LogFloatStats` for the display texture (tonemap-domain
    sanity vs HDR domain).
100. `[ ]` Final: after items 4/17, write the honest quality table
     (ReSTIR vs PT, static/rotating/Cornell, with sky fractions) into the
     README and retire the v215 numbers everywhere.

---

## Execution order

P0 4→10 after this review; P1 11–19 first (they gate the quality story);
P2 opportunistic. Re-visit this list after item 4: the honest PT comparison
may re-prioritize everything.
